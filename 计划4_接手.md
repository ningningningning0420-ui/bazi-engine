# 八字引擎 · 计划 4 接手开场白（复制到新 session 第一条消息）

> 把下面整段贴给新 session 即可接手。

---

接手「八字引擎」项目的**计划 4**。先按你的记忆系统加载 `project-bazi-engine` 记忆，再读仓库里这几份文档建立上下文，然后按既定节奏写计划 4 并执行。

## 项目是什么
确定性纯 TS 库 `bazi-core`：从 NPC 生辰确定性排八字盘 → 算命理「关系结构」（非性格结论）交给 LLM 长人物厚度；流年作暗线推手；不是算命器（不追求预测准，只要内部自洽+确定性可复现）。**核心红线：引擎里绝不存在「干支→性格词」查找表（否则=随机参数换皮）；八字是地基不是牢笼（一盘多解、只定下限不锁人格）；这些是后台结构事实，命理零泄漏（不进正文）。**

## 位置 / 仓库
- 项目根：`/Users/iris/Documents/SillyTavern/卡/我的/刀男人bot/八字引擎`（git repo，分支 main，配了 post-commit auto-push hook，commit 即推送）。
- 代码在 `bazi-core/`（ESM + vitest + zod + lunar-typescript[仅作历法 oracle]）。测试：`cd bazi-core && npx vitest run`；类型：`npx tsc --noEmit`。
- 设计文档 `设计文档.md`；**算法权威 `规格_L1B算法_计划3-4.md`（计划 4 的 combos/tension/pattern/cong 规格全在内，代码以此为准）**；已有计划 `实施计划_01/02/03_*.md`。

## 已完成（main 上，72 测试绿 / tsc 净）
- 计划 1 = L0 排盘：`computeChart(birthInput)→Chart`（四柱+藏干+五行/阴阳+日主+十神标注/计数）。
- 计划 2 = L1-A 结构事实：`分析.{通根, 旺衰[月令旺衰/得令得地得势/五行得分/同党异党/强弱比/身强弱+borderline], 刑冲合害[合冲刑害+贴邻+化神]}`。
- 计划 3 = L1-B 用神真相源链：`分析.{调候 ClimateResult, 涌现拓扑 EmergentTopology, 用神 FavorResult}`。**favorSign:Record<五行,-2..2> 是 L3/L4 一切吉凶的唯一真相源，已验 100% 确定性可复现。**
- 入口 `computeChartFacts(birthInput)→ChartFacts`（=Chart + 分析）。共享常量在 `src/constants/weights.ts`；`gan-zhi.ts` 有 sheng/ke/shengMe/keMe、WU_XING、各表；`types.ts` 是类型枢纽（转出 WuXing 等基础类型）。

## 计划 4 = L1-B 锚点结构（要做的）
在 `分析` 里再加 4 个字段（ChartAnalysis 现有 6 字段，加到 10；旧字段不动）：
1. **十神组合 combos**（`src/facts/combos.ts`，`detectGodCombos(facts)→ComboHit[]`）：12 个组合（伤官见官/伤官配印/食神制杀/财滋弱杀/财多身弱/印重身旺/杀重无制/杀印相生/食伤生财/比劫夺财/官印相生/伤官生财），**连续 `成立程度=gate(硬前提)×Π(透藏×旺衰×相邻×制化)`，抗查表**。
2. **矛盾张力 tension**（`src/facts/tension.ts`，`buildTension(涌现拓扑, 十神组合, 日主五行)→TensionResult`）：从相战对 + 内含矛盾组合派生轴 + 优缺同源点；派生层不重算。
3. **格局 pattern**（`src/facts/pattern.ts`，`buildPattern(facts)→PatternResult`）：月令本气透干机械立格 + 备选格；不 judge 成败高低清浊（留 LLM）。
4. **从格 cong**（`src/facts/cong.ts`，`buildCongSignal(facts)→CongSignal`）：只给从强/从弱信号+反例，最终定论留 LLM。
（调候 climate 计划 3 已做，pattern 复用既有；本计划不碰 L3/L4。）

## 计划 4 必带技术注意（承计划 2/3）
- combos 力量以 `force`（遍历四柱按 weights 累加、日柱天干排除）为准，**不用计数**；身强弱用 `bodyWeak01/bodyStrong01` 连续门，**删一切「身强/身弱」标签 gate**（防 0.55 踩线翻档=抗查表）；degree=gate×连续乘积；**计算顺序：伤官见官 必先于 官印相生**（后者抑制依赖前者）。
- tension 读 `clashes.intensity` 与 `combos.成立程度`，不重算；内含矛盾键集={伤官见官/财多身弱/杀重无制/印重身旺}。
- 三个共享权重（STEM_WEIGHT/HIDDEN_WEIGHT/MONTH_BRANCH_MULT）+ STRONG_CUT/WEAK_CUT **从 `constants/weights.ts` import，禁本地重声明**。
- 开放问题默认（已拍，可改）：用神十神只取主用神 / 内含矛盾不纳比劫夺财 / round2 只在输出字段。

## 工作节奏（沿用前三个计划）
1. 先 `superpowers:writing-plans` 写 `实施计划_04_锚点结构.md`（TDD 任务序 + 精确类型 + 每任务测试；算法体引用 `规格_L1B算法_计划3-4.md` 对应段）。提交计划到 main。
2. 建 feature 分支 `feat/plan-04-anchors` 执行；**每任务 TDD（先写失败测试→跑红→实现→跑绿→commit）**。
   - 注意：之前 Anthropic API 偶发 529 过载导致子代理 dispatch 挂掉；若遇到，改主控内联 TDD（exact code 按规格写、跑 vitest 验证），别死等。
   - commit 用 `git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "..."`，结尾带 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`；auto-push 自动推分支。
3. 全部任务完 + 全量 `npx vitest run && npx tsc --noEmit` 绿 → 派一个 **opus 终审子代理**（覆盖 spec 合规 + 抗查表[combos 真关系性非平表] + 命理自洽 + 与计划1/2/3 一致 + 代码质量）。
4. `superpowers:finishing-a-development-branch`：作者惯例=**ff 合并到 main + 删分支**（main+auto-push，不走 PR）；ff 合并不触发 hook，需手动 `git push origin main`。
5. 更新 `project-bazi-engine` 记忆（计划 4 完成 + 计划 5 接手注意）。

## 红线复述
引擎只算命理客观结构，解读交 LLM；favorSign 等数学侧 100% 确定性；combos 关系性+成立程度（不是有俩十神就成立）；不进正文、不锁人格、不写死性格表。

完事后下一步是计划 5（L3 流年推手 + L4 成功率系数），它会用上计划 3 的 favorSign + 计划 4 的 combos/tension。
