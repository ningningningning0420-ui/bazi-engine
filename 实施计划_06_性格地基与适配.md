# 八字引擎 · 计划 6：L2 性格地基（圣经校验）+ ports + generic-adapter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development 或 executing-plans。步骤用 `- [ ]`。
> 设计文档 §L2(195-210)三道校验 / §7 PersonaAnchors / §4.3 ports&adapters / §3.4 双寄存器+硬护栏 / §10 内容任务。**代码以本计划为准**（对设计文档的精确化）。已过 3-lens 对抗审核（红线 cite 不锁人格 / 工程确定性 / spec 一致），采纳 1 P0 + 2 P1 + 数 P2，见文末「设计决策与审核采纳」。
> **★作者拍板：通用先行·不专门为大奥适配·先写小卡试。** → 只做 generic-adapter（大奥-adapter 留未来），core 零大奥依赖，小卡能用 `ensurePersona`/`buildRuntimeContext`/`rollEvent` 跑通 L0–L4+L2 全链。**本计划=引擎层 only，UI/大奥对接另开（别让宿主假设渗 core）。**

**Goal:** 加 L2 性格地基——引擎提 L1 锚点（`extractCitableStructures`/`buildScripturePrompt`）+ 三道半 cite 校验（`validatePersonaAnchors`，只定下限不锁人格）+ 端口契约（ports）+ generic-adapter（圣经-writing 编排 + 台前命理-free 注入 + generic 掷骰）。v1 闭合。

**Architecture:** 引擎件全确定性纯函数（`extract`/`prompt`/`validate`）；`llmInvoke` 是 host 端 async，只在 adapter 编排里调。**cite 单一真相源** `extractCitableStructures(facts)`：prompt 把它逐字喂 LLM 当白名单，validate 用同一函数校验——cite 从"LLM 自由发挥 vs 引擎拼串"的对齐赌博，变成"从给定清单勾选"的确定性匹配。

**Tech Stack:** TypeScript（ESM）、vitest、zod（schema-first）、沿用 `bazi-core/`。

---

## 红线（每任务都守）
1. **cite 只定下限不锁人格**：三道校验只拒"违背结构"+"空洞/廉价矛盾"，**不要求唯一解**——同一伤官见官读成叛逆艺术家/腐败改革者/笑面权臣**全过**（只要 cite 了真实结构 + 优缺同源挂引擎识别的同源点 + 滋养向正向）。**身弱用印≠枭雄 等语义层故意不机器化**（机器化=干支→性格词查表=踩红线⑤）。
2. **双寄存器**：`buildScripturePrompt` 含命理（圣经-writing 后台）；`buildRuntimeContext` 命理-free（台前·禁词机器执法）+ 硬护栏。
3. **favorSign 不回写**：滋养向校验=`favorSign[w]≥1` 只读子集；引擎已算的 `优缺同源点` 是同源锚真相源（不让 LLM 重新发明）。
4. **引擎件 100% 确定性纯函数**；`llmInvoke` 非确定属 host，只在 adapter async 编排。
5. **引擎无"干支→性格词"映射**：枚举池 LLM 据圣经挑，引擎不从命局直接映射 anchors。
6. **改生辰=换 key=真重排盘**（§6.2）：ensurePersona 命中持久化必校验 birthInput 一致，fail-closed。

---

## 文件结构（计划 6）
```
bazi-core/src/persona/
  structures.ts  extractCitableStructures(facts) → CitableStructures（★cite 单一真相源）
  anchors.ts     PersonaAnchorsSchema(zod) + 枚举池 + MINGLI_FORBIDDEN 正则 + 类型
  prompt.ts      buildScripturePrompt(facts, priorViolations?) → ScripturePrompt
  validate.ts    validatePersonaAnchors(anchors, facts) → ValidationResult（四道·纯函数）
bazi-core/src/ports/index.ts     SeedPort/ClockPort/PersistencePort/LlmInvokePort + PersistedNpc
bazi-core/src/adapters/generic.ts  ensurePersona / buildRuntime / buildRuntimeContext / rollEvent
bazi-core/src/types.ts (+计划6类型)   bazi-core/src/index.ts (+计划6导出)
bazi-core/test/  structures / anchors / prompt / validate / ports? / generic-adapter / persona-integration .test.ts
```

## 类型（types.ts 计划 6）
```ts
// ===== 计划 6：L2 性格地基类型 =====
export const 主导驱力池 = ['求成就', '求安稳', '求自由', '求认同', '求掌控', '求联结', '求超越'] as const;
export const 对人基调池 = ['亲和', '疏离', '支配', '顺从', '戒备', '坦荡'] as const;
export const 命门池 = ['偏执', '优柔', '孤傲', '依赖', '急躁', '自抑'] as const;
export type 主导驱力 = (typeof 主导驱力池)[number];
export type 对人基调 = (typeof 对人基调池)[number];
export type 命门 = (typeof 命门池)[number];

export interface PersonaAnchors {
  主导驱力: 主导驱力; 对人基调: 对人基调; 命门: 命门;
  行为倾向标签: string[];
  滋养向: WuXing[];
  citedStructures: string[];
  优缺同源锚: { 优点cite: string; 弱点cite: string };
}
export interface CitableStructures { all: string[]; 优缺同源点: string[]; 张力轴: string[]; }
export interface ValidationViolation { check: string; reason: string; }
export interface ValidationResult { ok: boolean; violations: ValidationViolation[]; degraded: boolean; }
export interface ScripturePrompt {
  命局结构: string; 可cite白名单: string[]; 优缺同源候选: string[];
  写作任务: string; 双寄存器指令: string; cite要求: string; 一盘多解: string;
  硬护栏: string; priorViolations: string[];
}
export interface PersistedNpc { npcId: string; birthInput: BirthInput; personaAnchors: PersonaAnchors; scriptureText: string; }
```

---

### Task 1: `extractCitableStructures` + PersonaAnchors schema/枚举/禁词

**Files:** Create `src/persona/structures.ts`、`src/persona/anchors.ts`；Modify `src/types.ts`（+计划6类型）；Test `test/structures.test.ts`、`test/anchors.test.ts`

- [ ] **Step 1: 写失败测试**
```ts
// test/structures.test.ts
import { describe, expect, test } from 'vitest';
import { computeChartFacts } from '../src/analyze-chart';
import { extractCitableStructures } from '../src/persona/structures';

const facts = computeChartFacts({ year: 1990, month: 6, day: 15, hour: 10, hourUnknown: false, gender: '乾' });

describe('extractCitableStructures', () => {
  test('all 含 十神组合/张力轴/相战对/通关/格局·去重·确定性', () => {
    const c = extractCitableStructures(facts);
    expect(c.all.length).toBeGreaterThan(0);
    expect(c.all).toContain(facts.分析.格局.格);                    // 格局名
    for (const combo of facts.分析.十神组合) expect(c.all).toContain(combo.类型);
    expect(new Set(c.all).size).toBe(c.all.length);                 // 去重
    expect(extractCitableStructures(facts)).toEqual(c);             // 确定性
  });
  test('优缺同源点 = 引擎 矛盾张力.优缺同源点的 结构cite', () => {
    const c = extractCitableStructures(facts);
    expect(c.优缺同源点).toEqual([...new Set(facts.分析.矛盾张力.优缺同源点.map((p) => p.结构cite))]);
    for (const cite of c.优缺同源点) expect(c.all).toContain(cite); // 同源点必在白名单
  });
});
```
```ts
// test/anchors.test.ts
import { describe, expect, test } from 'vitest';
import { PersonaAnchorsSchema, MINGLI_FORBIDDEN, 主导驱力池 } from '../src/persona/anchors';

describe('PersonaAnchors schema + 禁词', () => {
  test('合法 anchors 通过 zod', () => {
    const ok = PersonaAnchorsSchema.safeParse({
      主导驱力: '求成就', 对人基调: '亲和', 命门: '偏执', 行为倾向标签: ['谨慎', '念旧'],
      滋养向: ['水', '木'], citedStructures: ['伤官见官'], 优缺同源锚: { 优点cite: '伤官见官', 弱点cite: '伤官见官' },
    });
    expect(ok.success).toBe(true);
  });
  test('非法枚举拒收', () => {
    expect(PersonaAnchorsSchema.safeParse({ 主导驱力: '乱填', 对人基调: '亲和', 命门: '偏执', 行为倾向标签: [], 滋养向: [], citedStructures: [], 优缺同源锚: { 优点cite: '', 弱点cite: '' } }).success).toBe(false);
  });
  test('MINGLI_FORBIDDEN 命中命理词', () => {
    expect(MINGLI_FORBIDDEN.test('食伤旺')).toBe(true);
    expect(MINGLI_FORBIDDEN.test('谨慎念旧')).toBe(false);
    expect(主导驱力池.length).toBe(7);
  });
});
```
- [ ] **Step 2: 跑红** `npx vitest run test/structures.test.ts test/anchors.test.ts` → FAIL。
- [ ] **Step 3: 实现** types.ts 加「类型」段；`structures.ts`：
```ts
import type { ChartFacts, CitableStructures } from '../types';
/** ★cite 单一真相源：prompt 当白名单喂 LLM·validate 据此校验（杜绝两侧拼串漂移）。 */
export function extractCitableStructures(facts: ChartFacts): CitableStructures {
  const an = facts.分析;
  const 组合 = an.十神组合.map((c) => c.类型);
  const 张力轴 = an.矛盾张力.矛盾张力轴.map((a) => a.轴名);
  const 相战 = an.涌现拓扑.clashes.map((c) => `${c.克}${c.受}相战`);
  const 通关 = an.涌现拓扑.tongGuanReport.filter((n) => n.status === '调和中').map((n) => `${n.node}通关`);
  const 通关缺 = an.涌现拓扑.tongGuanReport.filter((n) => n.status === '缺位待补').map((n) => `${n.node}通关缺位`);
  const 格局 = [an.格局.格];
  const all = [...new Set([...组合, ...张力轴, ...相战, ...通关, ...通关缺, ...格局])];
  return { all, 优缺同源点: [...new Set(an.矛盾张力.优缺同源点.map((p) => p.结构cite))], 张力轴 };
}
```
`anchors.ts`：
```ts
import { z } from 'zod';
import { WU_XING } from '../constants/gan-zhi';
import { 主导驱力池, 对人基调池, 命门池 } from '../types';
export { 主导驱力池, 对人基调池, 命门池 } from '../types';

/** 台前禁词：命理术语黑名单（行为倾向标签/隐藏暗示 须命理-free）。 */
export const MINGLI_FORBIDDEN = /[木火土金水]|官杀|正官|七杀|偏财|正财|财星?|偏印|正印|印星?|食神|伤官|食伤|比肩|劫财|比劫|旺衰|身强|身弱|用神|喜神|忌神|流年|大运|流月|刑冲|合化|通关|调候|格局|favorSign/;

export const PersonaAnchorsSchema = z.object({
  主导驱力: z.enum(主导驱力池), 对人基调: z.enum(对人基调池), 命门: z.enum(命门池),
  行为倾向标签: z.array(z.string()),
  滋养向: z.array(z.enum(WU_XING as unknown as [string, ...string[]])),
  citedStructures: z.array(z.string()),
  优缺同源锚: z.object({ 优点cite: z.string(), 弱点cite: z.string() }),
});
```
> 注：`WU_XING` 是 `readonly WuXing[]`；`z.enum` 需元组，用 `as unknown as [string,...]`（运行时校验五行字面值）。
- [ ] **Step 4: 跑绿** `npx vitest run test/structures.test.ts test/anchors.test.ts && npx tsc --noEmit` → PASS。
- [ ] **Step 5: commit** `feat: 计划6 extractCitableStructures(cite单一真相源) + PersonaAnchors zod schema + 台前禁词`

---

### Task 2: `buildScripturePrompt`

**Files:** Create `src/persona/prompt.ts`；Test `test/prompt.test.ts`

提 L1 锚点喂命师 LLM。**含命理（后台 prompt·非正文）**；把 `extractCitableStructures(facts).all` 逐字当白名单交 LLM，明令 `citedStructures` 只能从白名单原样复制。

- [ ] **Step 1: 写失败测试**
```ts
import { describe, expect, test } from 'vitest';
import { computeChartFacts } from '../src/analyze-chart';
import { buildScripturePrompt } from '../src/persona/prompt';
import { extractCitableStructures } from '../src/persona/structures';

const facts = computeChartFacts({ year: 1990, month: 6, day: 15, hour: 10, hourUnknown: false, gender: '乾' });

describe('buildScripturePrompt', () => {
  test('可cite白名单 === extractCitableStructures.all（单一真相源）', () => {
    const p = buildScripturePrompt(facts);
    expect(p.可cite白名单).toEqual(extractCitableStructures(facts).all);
  });
  test('含命局结构(命理) + 硬护栏 + 双寄存器/一盘多解指令', () => {
    const p = buildScripturePrompt(facts);
    expect(p.命局结构).toMatch(/用神|十神组合|矛盾张力|涌现拓扑|格局/);
    expect(p.硬护栏.length).toBeGreaterThan(0);
    expect(p.双寄存器指令).toMatch(/后台|台前/);
    expect(p.一盘多解).toMatch(/多解|不锁/);
  });
  test('priorViolations 透传(retry 用) + 确定性', () => {
    const p = buildScripturePrompt(facts, ['cite了不存在的结构X']);
    expect(p.priorViolations).toEqual(['cite了不存在的结构X']);
    expect(buildScripturePrompt(facts)).toEqual(buildScripturePrompt(facts));
  });
});
```
- [ ] **Step 2: 跑红** → FAIL。
- [ ] **Step 3: 实现 `prompt.ts`**：从 facts.分析 提（用神{主用神/喜/忌/favorSign}、十神组合[{类型,成立程度,极性}]、矛盾张力轴[{轴名,两极}]、涌现拓扑{clashes/tongGuan/flow/dominant/missing}、**旺衰{身强弱,borderline}**[★borderline 在旺衰非用神]、调候、格局）拼 `命局结构` 串；`可cite白名单=extractCitableStructures(facts).all`；`优缺同源候选=extractCitableStructures(facts).优缺同源点`；写作任务/双寄存器指令/cite要求（citedStructures 只从白名单原样复制·≥min(3,白名单大小)·每条张力轴在成长弧/弱点展开·优缺同源锚从优缺同源候选挑一个）/一盘多解（同结构多自洽读法·选一条即可·绝不回写 favorSign）/硬护栏（§3.4）/priorViolations。纯函数。
- [ ] **Step 4: 跑绿** → PASS + tsc。
- [ ] **Step 5: commit** `feat: buildScripturePrompt(L1锚点+cite白名单逐字喂LLM+双寄存器+一盘多解+硬护栏·纯函数)`

---

### Task 3: `validatePersonaAnchors`（四道·★只定下限不锁人格）

**Files:** Create `src/persona/validate.ts`；Test `test/validate.test.ts`

四道：①子集（滋养向 favorSign≥1）②cite（≥min(N,|valid|)·全∈valid）③优缺同源（同一锚·∈引擎优缺同源点·**贫盘 degrade**）④台前纯净（行为倾向标签命理-free）。

- [ ] **Step 1: 写失败测试**
```ts
import { describe, expect, test } from 'vitest';
import { computeChartFacts } from '../src/analyze-chart';
import { validatePersonaAnchors } from '../src/persona/validate';
import { extractCitableStructures } from '../src/persona/structures';
import type { PersonaAnchors } from '../src/types';

const facts = computeChartFacts({ year: 1990, month: 6, day: 15, hour: 10, hourUnknown: false, gender: '乾' });
const cit = extractCitableStructures(facts);
const 喜 = facts.分析.用神.喜;
const 同源 = cit.优缺同源点[0] ?? cit.all[0]!;
const base = (): PersonaAnchors => ({
  主导驱力: '求成就', 对人基调: '亲和', 命门: '偏执', 行为倾向标签: ['谨慎'],
  滋养向: 喜.slice(0, 1), citedStructures: cit.all.slice(0, Math.min(3, cit.all.length)),
  优缺同源锚: { 优点cite: 同源, 弱点cite: 同源 },
});

describe('validatePersonaAnchors', () => {
  test('合法 anchors → ok（含同源点时锚之）', () => {
    const a = base();
    if (cit.优缺同源点.length > 0) a.citedStructures = [...new Set([...a.citedStructures, cit.优缺同源点[0]!])];
    const r = validatePersonaAnchors(a, facts);
    expect(r.ok).toBe(true);
  });
  test('★不锁人格：同一白名单两套不同 anchors(不同驱力/标签) 都过', () => {
    const a1 = base(); const a2 = { ...base(), 主导驱力: '求自由' as const, 命门: '孤傲' as const, 行为倾向标签: ['叛逆'] };
    if (cit.优缺同源点.length > 0) { a1.citedStructures.push(cit.优缺同源点[0]!); a2.citedStructures.push(cit.优缺同源点[0]!); }
    expect(validatePersonaAnchors(a1, facts).ok).toBe(true);
    expect(validatePersonaAnchors(a2, facts).ok).toBe(true);
  });
  test('cite 盘上不存在的结构 → 违规', () => {
    const a = base(); a.citedStructures = ['根本不存在的结构xyz'];
    expect(validatePersonaAnchors(a, facts).ok).toBe(false);
  });
  test('滋养向含忌神(favorSign<1) → 违规', () => {
    const 忌 = (['木','火','土','金','水'] as const).find((w) => facts.分析.用神.favorSign[w] < 1);
    if (忌) { const a = base(); a.滋养向 = [忌]; expect(validatePersonaAnchors(a, facts).ok).toBe(false); }
  });
  test('优缺同源锚不同源 → 违规(有同源点时)', () => {
    if (cit.优缺同源点.length > 0) {
      const a = base(); a.优缺同源锚 = { 优点cite: cit.all[0]!, 弱点cite: cit.all[1] ?? cit.all[0]! };
      a.优缺同源锚.弱点cite = '不同的cite' === a.优缺同源锚.优点cite ? cit.all[0]! : (cit.all[1] ?? '别的');
      // 构造两 cite 不同
      a.优缺同源锚 = { 优点cite: cit.优缺同源点[0]!, 弱点cite: cit.all.find((s) => s !== cit.优缺同源点[0]) ?? 'x' };
      expect(validatePersonaAnchors(a, facts).ok).toBe(false);
    }
  });
  test('台前纯净：行为倾向标签含命理词 → 违规', () => {
    const a = base(); a.行为倾向标签 = ['食伤旺'];
    if (cit.优缺同源点.length > 0) a.citedStructures.push(cit.优缺同源点[0]!);
    const r = validatePersonaAnchors(a, facts);
    expect(r.violations.some((v) => v.check === '台前纯净')).toBe(true);
  });
  test('确定性', () => {
    expect(validatePersonaAnchors(base(), facts)).toEqual(validatePersonaAnchors(base(), facts));
  });
});
```
- [ ] **Step 2: 跑红** → FAIL。
- [ ] **Step 3: 实现 `validate.ts`**
```ts
import { extractCitableStructures } from './structures';
import { MINGLI_FORBIDDEN } from './anchors';
import type { ChartFacts, PersonaAnchors, ValidationResult, ValidationViolation } from '../types';

export const CITE_MIN_N = 3;

/** 三道半校验（只定下限不锁人格）。①子集 ②cite ③优缺同源(贫盘降级) ④台前纯净。 */
export function validatePersonaAnchors(anchors: PersonaAnchors, facts: ChartFacts): ValidationResult {
  const violations: ValidationViolation[] = [];
  let degraded = false;
  const cit = extractCitableStructures(facts);
  const validSet = new Set(cit.all);
  const favorSign = facts.分析.用神.favorSign;

  // ① 子集（滋养向 favorSign≥1·只读不回写 favorSign）— 身弱用印≠枭雄属语义层·故意不机器化(机器化=查表=踩红线⑤)
  for (const w of anchors.滋养向) if ((favorSign[w] ?? 0) < 1) violations.push({ check: '子集', reason: `滋养向 ${w} 非正向用神/喜(favorSign<1)` });

  // ② cite（≥min(N,|valid|)·全∈valid·不要求覆盖全部张力轴=不锁人格）
  const effN = Math.min(CITE_MIN_N, validSet.size);
  if (anchors.citedStructures.length < effN) violations.push({ check: 'cite', reason: `citedStructures 少于 ${effN} 条(空洞)` });
  for (const s of anchors.citedStructures) if (!validSet.has(s)) violations.push({ check: 'cite', reason: `cite 了盘上不存在的结构「${s}」` });

  // ③ 优缺同源（须命中引擎识别的优缺同源点·同一锚·贫盘无同源点→降级豁免）
  const 同源池 = new Set(cit.优缺同源点);
  if (同源池.size === 0) { degraded = true; }
  else {
    const { 优点cite, 弱点cite } = anchors.优缺同源锚;
    if (优点cite !== 弱点cite) violations.push({ check: '优缺同源', reason: '优点/弱点 cite 不同源(廉价矛盾)' });
    else if (!同源池.has(优点cite)) violations.push({ check: '优缺同源', reason: `同源锚「${优点cite}」非引擎识别的优缺同源点` });
  }

  // ④ 台前纯净（行为倾向标签命理-free·红线②机器执法）
  for (const tag of anchors.行为倾向标签) if (MINGLI_FORBIDDEN.test(tag)) violations.push({ check: '台前纯净', reason: `行为倾向标签「${tag}」含命理词` });

  return { ok: violations.length === 0, violations, degraded };
}
```
- [ ] **Step 4: 跑绿** → PASS + tsc。
- [ ] **Step 5: commit** `feat: validatePersonaAnchors 四道(子集/cite/优缺同源锚引擎事实/台前禁词·只定下限不锁人格·贫盘降级)`

---

### Task 4: ports 接口契约

**Files:** Create `src/ports/index.ts`；Test `test/ports.test.ts`（仅类型/形状烟雾）

- [ ] **Step 1: 写失败测试**
```ts
import { describe, expect, test } from 'vitest';
import type { SeedPort, ClockPort, PersistencePort, LlmInvokePort, PersistedNpc } from '../src/ports';
describe('ports 形状', () => {
  test('PersistedNpc + 端口可实现(编译即验)', () => {
    const mem = new Map<string, PersistedNpc>();
    const p: PersistencePort = { load: (id) => mem.get(id) ?? null, save: (n) => void mem.set(n.npcId, n) };
    const clock: ClockPort = { now: () => ({ year: 2025, month: 1, day: 1 }) };
    const seed: SeedPort = { hash: (s) => s.length };
    expect(p.load('x')).toBeNull();
    expect(clock.now().year).toBe(2025);
    expect(seed.hash('ab')).toBe(2);
    const _llm: LlmInvokePort = { writeScripture: async () => ({ anchors: {}, scriptureText: '' }) };
    expect(typeof _llm.writeScripture).toBe('function');
  });
});
```
- [ ] **Step 2: 跑红** → FAIL。
- [ ] **Step 3: 实现 `ports/index.ts`**
```ts
import type { AtDate, BirthInput, PersonaAnchors, ScripturePrompt } from '../types';
export interface PersistedNpc { npcId: string; birthInput: BirthInput; personaAnchors: PersonaAnchors; scriptureText: string; }
export interface SeedPort { hash(input: string): number; }              // 架空/worldEpoch(消费 v2·接口先定)
export interface ClockPort { now(): AtDate; }                           // 可选·当前剧情日期·不提供=L3/L4 关
export interface PersistencePort { load(npcId: string): PersistedNpc | null; save(npc: PersistedNpc): void; }
export interface LlmInvokePort { writeScripture(prompt: ScripturePrompt): Promise<{ anchors: unknown; scriptureText: string }>; }
```
> `anchors: unknown`——adapter 先 `PersonaAnchorsSchema.safeParse` 再 validate（LLM 可能产非法 enum）。
- [ ] **Step 4: 跑绿** → PASS + tsc。
- [ ] **Step 5: commit** `feat: ports 端口契约(seed/clock可选/persistence/llmInvoke·core不实现·host注入)`

---

### Task 5: generic-adapter（编排 + 台前注入 + generic 掷骰）

**Files:** Create `src/adapters/generic.ts`；Test `test/generic-adapter.test.ts`

- `ensurePersona(npcId, birthInput, ports) → Promise<PersistedNpc>`：load 命中→**校验 birthInput 一致**(不等 fail-closed)→返回；否则 computeChartFacts→循环≤MAX_RETRY(3)：buildScripturePrompt(facts, priorViolations)→llm.writeScripture→**PersonaAnchorsSchema.safeParse**(失败→issues 进 priorViolations continue)→validatePersonaAnchors(ok→save+返回·否则 violations.reason 进 priorViolations)→超限 throw。
- `buildRuntimeContext(persona, tendency?) → string`：★台前·命理-free·拼 主导驱力/对人基调/命门/行为倾向标签 + tendency?.隐藏暗示 + 硬护栏。
- `rollEvent(chart, atDate, eventKind, prng) → RollResult`：`rollWithCoeff(eventModifier(chart, atDate, eventKind), prng)`。

- [ ] **Step 1: 写失败测试**
```ts
import { describe, expect, test } from 'vitest';
import { computeChart } from '../src/compute-chart';
import { computeChartFacts } from '../src/analyze-chart';
import { ensurePersona, buildRuntimeContext, rollEvent } from '../src/adapters/generic';
import { extractCitableStructures } from '../src/persona/structures';
import { MINGLI_FORBIDDEN } from '../src/persona/anchors';
import type { BirthInput, PersistedNpc, PersonaAnchors, AtDate } from '../src/types';
import type { PersistencePort, LlmInvokePort } from '../src/ports';

const BI: BirthInput = { year: 1990, month: 6, day: 15, hour: 10, hourUnknown: false, gender: '乾' };
const facts = computeChartFacts(BI);
const cit = extractCitableStructures(facts);
const 同源 = cit.优缺同源点[0];
function goodAnchors(): PersonaAnchors {
  const cited = cit.all.slice(0, Math.min(3, cit.all.length));
  if (同源 && !cited.includes(同源)) cited.push(同源);
  return { 主导驱力: '求成就', 对人基调: '亲和', 命门: '偏执', 行为倾向标签: ['谨慎'],
    滋养向: facts.分析.用神.喜.slice(0, 1), citedStructures: cited,
    优缺同源锚: { 优点cite: 同源 ?? cited[0]!, 弱点cite: 同源 ?? cited[0]! } };
}
const memPersist = (): PersistencePort => { const m = new Map<string, PersistedNpc>(); return { load: (id) => m.get(id) ?? null, save: (n) => void m.set(n.npcId, n) }; };

describe('generic-adapter', () => {
  test('ensurePersona：LLM 产合法 anchors → save + 返回', async () => {
    const persistence = memPersist();
    const llm: LlmInvokePort = { writeScripture: async () => ({ anchors: goodAnchors(), scriptureText: '圣经全文…' }) };
    const npc = await ensurePersona('npc1', BI, { persistence, llm } as any);
    expect(npc.personaAnchors.主导驱力).toBe('求成就');
    expect(persistence.load('npc1')).not.toBeNull();
  });
  test('ensurePersona：命中持久化但 birthInput 不符 → fail-closed throw', async () => {
    const persistence = memPersist();
    const llm: LlmInvokePort = { writeScripture: async () => ({ anchors: goodAnchors(), scriptureText: 'x' }) };
    await ensurePersona('npc1', BI, { persistence, llm } as any);
    await expect(ensurePersona('npc1', { ...BI, year: 1991 }, { persistence, llm } as any)).rejects.toThrow();
  });
  test('ensurePersona：LLM 持续产非法 anchors → retry 耗尽 throw', async () => {
    const persistence = memPersist();
    const llm: LlmInvokePort = { writeScripture: async () => ({ anchors: { 主导驱力: '乱填' }, scriptureText: 'x' }) };
    await expect(ensurePersona('npc2', BI, { persistence, llm } as any)).rejects.toThrow();
  });
  test('★buildRuntimeContext 命理-free(禁词) + 含硬护栏', () => {
    const ctx = buildRuntimeContext(goodAnchors(), { 隐藏暗示: '近期在事业上较有起色' } as any);
    expect(ctx).not.toMatch(MINGLI_FORBIDDEN);
    expect(ctx).toMatch(/护栏|命理|内部依据/);
  });
  test('rollEvent = eventModifier→rollWithCoeff(prng外置·确定性)', () => {
    const chart = computeChart(BI);
    const at: AtDate = { year: 2025, month: 6, day: 15 };
    const r = rollEvent(chart, at, '求财', () => 0.1);
    expect(typeof r.success).toBe('boolean');
    expect(r).toEqual(rollEvent(chart, at, '求财', () => 0.1));
  });
});
```
- [ ] **Step 2: 跑红** → FAIL。
- [ ] **Step 3: 实现 `adapters/generic.ts`**（ensurePersona async 编排 + safeParse + birthInput guard；buildRuntimeContext 台前命理-free + 硬护栏 HARDGUARD 常量；rollEvent）。`birthInputEqual` 逐字段比较。
- [ ] **Step 4: 跑绿** → PASS + tsc。
- [ ] **Step 5: commit** `feat: generic-adapter ensurePersona(编排+safeParse+birthInput防呆)+buildRuntimeContext(台前命理-free+硬护栏)+rollEvent`

---

### Task 6: index 导出 + persona 集成测试

**Files:** Modify `src/index.ts`；Test `test/persona-integration.test.ts`

- [ ] **Step 1: 写失败测试**：`test/persona-integration.test.ts` 从 `../src/index` 导入全部计划6 符号；端到端：computeChart→ensurePersona(mock llm 产 valid)→buildRuntimeContext 命理-free→rollEvent；断言全链通 + 台前禁词 + 确定性。
- [ ] **Step 2: 跑红** → FAIL。
- [ ] **Step 3: 实现** index.ts 加计划6导出块（extractCitableStructures/buildScripturePrompt/validatePersonaAnchors/PersonaAnchorsSchema/MINGLI_FORBIDDEN + ensurePersona/buildRuntimeContext/rollEvent + ports 类型 + 全部计划6类型）。
- [ ] **Step 4: 跑绿（全量）** `npx vitest run && npx tsc --noEmit` → 全绿（141 + 计划6）。
- [ ] **Step 5: commit** `feat: 计划6导出 + persona 集成(L2性格地基+ports+generic-adapter·v1闭合)`

---

## Self-Review（写后自查）
- 覆盖 §L2 三道校验（子集/cite/优缺同源·+台前纯净第四道）+ §7 PersonaAnchors + §4.3 ports/generic-adapter + §3.4 双寄存器/硬护栏。
- 红线：cite 单一真相源 extractCitableStructures（prompt/validate 共用·杜绝漂移）；不锁人格（drop 全消费·优缺同源锚引擎事实·贫盘降级·语义层不机器化·同白名单多 anchors 全过测试）；favorSign 只读（滋养向 favorSign≥1）；引擎件纯函数 sync·llm async 仅 adapter；台前禁词机器执法；birthInput fail-closed。
- 确定性：extract/prompt/validate 纯函数·每模块"同输入深相等"测试；adapter retry 用 safeParse+violations 注入。
- 通用先行：generic-adapter 零大奥依赖·小卡端到端可跑（集成测试）；大奥-adapter/worldEpoch 消费→未来/v2。

## 设计决策与审核采纳（3-lens 审核 + 采纳）
1. **[P0] cite 单一真相源**：`extractCitableStructures(facts)` 抽出·buildScripturePrompt 逐字当白名单喂 LLM（citedStructures 只能原样复制）·validate 据同函数校验。消除"LLM 自由发挥 vs 引擎拼串"误拒主路径。
2. **[P1] 不锁人格·drop 全消费**：校验②不再要求覆盖全部张力轴（过严挤压多解）·改 `≥min(CITE_MIN_N,|valid|)`；张力轴消费降为 prompt 写作指令（LLM 侧软约束）。
3. **[P1] 优缺同源锚=引擎事实**：校验③要求优缺同源锚 ∈ `facts.分析.矛盾张力.优缺同源点`（复用承重墙·不让 LLM 重新发明）·**贫盘(无同源点)degrade 豁免**·CITE_MIN_N clamp `min(3,|valid|)`（贫盘不 throw）。
4. **[P2] 台前禁词第四道**：validatePersonaAnchors 加"台前纯净"——行为倾向标签命理-free（MINGLI_FORBIDDEN 机器执法红线②）。
5. **[P2] adapter 编排健壮**：ensurePersona 先 `PersonaAnchorsSchema.safeParse`(非法 enum 进 retry)·再 validate·violations 注入 priorViolations·birthInput 不符 fail-closed·MAX_RETRY=3 超限 throw。
6. **[P2/P3]** rollEvent 收 `chart`(非 facts·对齐 eventModifier)；子集校验用 favorSign≥1(不依赖 喜[] 含主用神)；borderline 从 `旺衰`(非用神)取；通关白名单含"调和中"+"缺位待补"(后者戏剧张力)；PersistedNpc 内联 scriptureText(偏离 §7 scriptureRef·小卡无 blob·回写设计文档)；retry 文案只述客观缺失不投喂答案；clock 可选(L3/L4 由 host 传不传 atDate gate)。

**开放问题（默认·可改）**：①CITE_MIN_N=3(内容旋钮·已 clamp)·枚举池通用 v1 可扩展。②张力轴"在成长弧/弱点消费"机器代理为 cite 覆盖+prompt 指令(不做 NLP)。③worldEpoch/SeedPort 消费 v2(小卡真公历)。④大奥-adapter(缘起/裁决/真蝴蝶 gate)→未来。

## Execution Handoff
作者既定节奏=主控内联 TDD（计划1-5 已用）：逐 Task 红→实现→绿→commit；全绿+tsc 净后派多 agent 终审(红线/确定性/一致)+性质测试；ff 合并 main+删分支。**v1 闭合后→可写小卡试整套八字系统（L0-L4+L2 全链）。**
