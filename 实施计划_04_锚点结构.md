# 八字引擎 · 计划 4：L1-B 锚点结构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development 或 executing-plans 逐任务实现。步骤用 `- [ ]` 复选框。
> 算法权威 = `规格_L1B算法_计划3-4.md`（combos/tension/pattern/cong 段）。该规格只给了**结构**（degree=gate×Π(透藏×旺衰×相邻×制化)、helper 列表、极性、计算顺序约束、少数 gate），**没给每个组合的精确公式**（"详见研究定稿"，研究定稿不在仓库）。本计划据规格结构 + 子平命理 + 抗查表红线**补全 12 组合精确公式**，并已过一轮 4-lens 对抗审核（抗查表/命理自洽/工程确定性/跨计划一致）——审核结论 + 采纳的修正见文末「设计决策与审核采纳」。**代码以本计划的公式为准（本计划是对规格的精确化，无冲突）。**

**Goal:** 在计划 3 的 `ChartAnalysis` 上确定性算出 **十神组合 combos / 矛盾张力 tension / 格局 pattern / 从格信号 cong** 四个锚点结构字段，全部抗查表（连续+关系性，不是"有俩十神在场就成立"）、100% 可复现、命理零泄漏、只定下限不锁人格。

**Architecture:** 纯函数。在 `analyze-chart.ts` 现有装配链（…→favor）后追加 `pattern → cong → combos → tension`（favor 不依赖这 4 个，放其后不破依赖；tension 依赖 combos+topology，故 combos 先于 tension）。各 builder 传**已算好的片段**（four/dm/strength/roots/topology），与 `analyzeFavor` 范式一致，不传整个 facts 黑盒。共享 `categoryOf(w,dmW)` 提取到 `ten-gods.ts`，topology/favor/tension 三方复用。

**Tech Stack:** TypeScript（ESM）、vitest、沿用 `bazi-core/`。

---

## 红线（每个任务都要守）

1. **抗查表**：每个 combo 的 `成立程度` 是 `gate(结构在场二元) × 透藏 × 旺衰 × 相邻 × 制化` 的连续乘积，"制化"维必引入第三/第四方关系神（同一七杀在 食神制杀/杀印相生/杀重无制/财滋弱杀 因制化神不同 degree 完全不同）。**删一切"身强/身弱"标签 gate**，改 `bodyStrong01/bodyWeak01` 连续门。自检：把八字核换成 `roll()` 随机参数，degree 有无实质变化？无=已退化。
2. **100% 确定性**：无 RNG/Date/全局态；所有并列必有确定 tie-break（复用 favor.ts 的 `WUXING_TIE_ORDER`/`pickMaxByScore` 范式）；`round2` **只在最外层输出字段**，中间全精度。
3. **命理零泄漏 / 只定下限不锁人格**：所有字符串是命理-结构串（无性格词）；这些是后台结构事实，最终人物由 LLM 长。

---

## 文件结构（计划 4）

```
bazi-core/src/
  ten-gods.ts          (改：+ 导出 categoryOf(w,dmW))
  facts/topology.ts    (改：删私有 categoryOf，import 共享版)
  facts/favor.ts       (改：删私有 catOf，import 共享版)
  facts/combos.ts      (新：detectGodCombos + 共享 helper)
  facts/tension.ts     (新：buildTension)
  facts/pattern.ts     (新：buildPattern)
  facts/cong.ts        (新：buildCongSignal)
  analyze-chart.ts     (改：装配 4 个新模块)
  types.ts             (改：+ 计划4 类型；ChartAnalysis +4 字段)
  index.ts             (改：+ 计划4 导出)
bazi-core/test/
  ten-gods.test.ts     (扩：categoryOf)
  combos.test.ts (新)  tension.test.ts (新)  pattern.test.ts (新)  cong.test.ts (新)
  analyze-chart.test.ts(扩：4 字段集成)
```

## 类型（types.ts 计划 4 新增；ChartAnalysis 加 4 字段，旧 6 字段不动）

```ts
// ===== 计划 4：L1-B 锚点结构类型 =====

export type GodComboKind =
  | '伤官见官' | '伤官配印' | '食神制杀' | '财滋弱杀' | '财多身弱' | '印重身旺'
  | '杀重无制' | '杀印相生' | '食伤生财' | '比劫夺财' | '官印相生' | '伤官生财';
export type ComboPolarity = '吉向' | '凶向' | '中性';
export interface ComboMember { 十神: TenGod | TenGodCategory; force: number; transparency: number; }
export interface ComboHit {
  类型: GodComboKind;
  成立程度: number;            // round2 输出值
  极性: ComboPolarity;
  内含矛盾: boolean;           // ∈{伤官见官,财多身弱,杀重无制,印重身旺}
  members: ComboMember[];
  力量快照: Record<string, number>;  // 全精度：含核心十神 force + '制化系数'(制化维乘子) + '原始成立度'
  说明键: string;             // 命理-结构串，无性格词
}

export type GeName =
  | '建禄格' | '羊刃格' | '月劫格'
  | '食神格' | '伤官格' | '正财格' | '偏财格' | '正官格' | '七杀格' | '正印格' | '偏印格';
export type LiGeBasis = '建禄' | '羊刃' | '月劫' | '本气透干' | '杂气透干' | '月令本气暗藏未透';
export interface PatternResult {
  格: GeName; 立格依据: LiGeBasis; 透干: boolean;
  月令本气十神: TenGod; 备选格: GeName[]; 说明: string;
}

export interface CongSignal {
  候选: '从强' | '从弱' | null;
  强度: number;               // round2；候选 null → 0
  触发项: string[];
  反例: string[];
}

export interface TensionAxis {
  轴名: string;
  来源类型: '相战' | '内含矛盾' | '元张力';
  来源cite: string;
  两极: [string, string];
  张力强度: number;           // round2 输出值
  优缺同源: boolean;
}
export interface SameSourcePoint { 结构cite: string; 同一来源: string; 正面表述: string; 负面表述: string; }
export interface TensionResult { 矛盾张力轴: TensionAxis[]; 优缺同源点: SameSourcePoint[]; }

// ChartAnalysis 计划4 追加（旧 6 字段不动）：
//   十神组合: ComboHit[]; 矛盾张力: TensionResult; 格局: PatternResult; 从格信号: CongSignal;
```

---

### Task 1: 共享 `categoryOf` 提取 + 计划4 类型骨架

把 topology/favor 各自私有的"五行→日主十神类别"映射提取到 `ten-gods.ts` 单一来源（消除三套漂移风险，tension 也要用），并落地计划4 全部独立类型（暂不动 ChartAnalysis，留 Task 7 装配时加，避免中途编译断裂）。

**Files:**
- Modify: `bazi-core/src/ten-gods.ts`（+ `categoryOf`）
- Modify: `bazi-core/src/facts/topology.ts`（删私有 `categoryOf`，import 共享版）
- Modify: `bazi-core/src/facts/favor.ts`（删私有 `catOf`，import 共享版）
- Modify: `bazi-core/src/types.ts`（+ 计划4 独立类型；**先不动 ChartAnalysis**）
- Test: `bazi-core/test/ten-gods.test.ts`（+ categoryOf）

- [ ] **Step 1: 写失败测试**（追加到 `test/ten-gods.test.ts`）

```ts
import { categoryOf } from '../src/ten-gods';

describe('categoryOf 五行→日主十神类别', () => {
  test('甲(木)日主：木比劫/水印/火食伤/土财/金官杀', () => {
    expect(categoryOf('木', '木')).toBe('比劫');
    expect(categoryOf('水', '木')).toBe('印');
    expect(categoryOf('火', '木')).toBe('食伤');
    expect(categoryOf('土', '木')).toBe('财');
    expect(categoryOf('金', '木')).toBe('官杀');
  });
  test('戊(土)日主：土比劫/火印/金食伤/水财/木官杀', () => {
    expect(categoryOf('土', '土')).toBe('比劫');
    expect(categoryOf('火', '土')).toBe('印');
    expect(categoryOf('金', '土')).toBe('食伤');
    expect(categoryOf('水', '土')).toBe('财');
    expect(categoryOf('木', '土')).toBe('官杀');
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/ten-gods.test.ts` → FAIL（categoryOf 未导出）。

- [ ] **Step 3: 实现**

`ten-gods.ts` 追加（import 补 `shengMe, keMe, type WuXing`）：

```ts
import { GAN_WUXING, GAN_YINYANG, sheng, ke, shengMe, keMe, type Gan, type WuXing } from './constants/gan-zhi';
// ...既有 tenGod / category 不动...

/** 五行 w 相对日主五行 dmW 的十神类别（topology/favor/tension 单一来源）。 */
export function categoryOf(w: WuXing, dmW: WuXing): TenGodCategory {
  if (w === dmW) return '比劫';
  if (w === shengMe(dmW)) return '印';
  if (w === sheng(dmW)) return '食伤';
  if (w === ke(dmW)) return '财';
  return '官杀'; // keMe(dmW)
}
```

`topology.ts`：删除文件内私有 `categoryOf`（19–25 行那个），改 `import { categoryOf } from '../ten-gods';`；调用处 `categoryOf(w, dmW)` 不变。`favor.ts`：删除私有 `catOf`（42–43 行），import 共享 `categoryOf`，把 `catOf(主用神)` 改为 `categoryOf(主用神, dmW)`。

`types.ts`：在「计划 3」类型块后追加上面「类型」段列出的**全部计划4 独立类型**（GodComboKind…TensionResult），**ChartAnalysis 暂不改**。

- [ ] **Step 4: 跑绿** — `npx vitest run test/ten-gods.test.ts test/topology.test.ts test/favor.test.ts && npx tsc --noEmit` → 全 PASS（topology/favor 回归不变）。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/ten-gods.ts bazi-core/src/facts/topology.ts bazi-core/src/facts/favor.ts bazi-core/src/types.ts bazi-core/test/ten-gods.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: 提取共享 categoryOf 到 ten-gods + 计划4 锚点结构类型骨架

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: combos 共享 helper（`combos.ts` 上半）

实现 12 组合公式所需的全部 helper。**这是抗查表 + 确定性的承重墙**，先单独测死再写组合。

**Files:** Create `bazi-core/src/facts/combos.ts`；Test `bazi-core/test/combos.test.ts`

**helper 精确语义（落地必须逐条照此，审核已焊死歧义）：**

- `force(g: TenGod): number` 原始累加（**无 clamp**）：遍历 `SLOT_ORDER` 的 present 柱——天干用 `p.ganGod`（**排除日柱天干** `four.日.ganGod`='比肩'占位=日主自身；日支藏干正常计入），命中 `+STEM_WEIGHT`；藏干用 `h.god`，命中 `+HIDDEN_WEIGHT[h.role]`，**月支藏干 ×MONTH_BRANCH_MULT**。三常量从 `constants/weights.ts` import。一次性算出 `Record<TenGod, number>`。
- `categoryForce(c: TenGodCategory): number` = 该类别下各十神 force 之和。一次性算出 `Record<TenGodCategory, number>`。
- `fn(x: number): number = clamp01(x / FORCE_REF)`，`FORCE_REF=2.0`。（入参是 raw force / categoryForce。）
- `cp(target): number` 制化神出场度，**按 target 类型分派**：`TenGodCategory → clamp01(categoryForce(target)/CTRL_REF)`；`TenGod → clamp01(force(target)/CTRL_REF)`。`CTRL_REF=2.0`。
- `transparencyFactor(g: TenGod): number` 纯离散梯度（**不受柱位/月支倍率影响**）：
  - 透干分支：非日柱天干里 `p.ganGod===g` 的柱集；若集非空 → revealed。rooted = 这些柱中 ∃ 一柱其 `StemRoots`（按 `slot && stem` 精确匹配，**不按五行模糊匹配**）`hasRoot===true`。透根→1.0，透无根→0.7。
  - 无透干分支：遍历各支 `HIDDEN_STEMS`，`h.god===g`：有 `本气`→0.5，仅 `中气/余气`→0.3，皆无→0。**藏干不查 StemRoots。**
  - `catTransparency(c) = max(该类别下各十神 transparencyFactor)`。
- `representativeSlot(target): PillarSlot | null` 主位：
  - 单神：透出的非日柱槽（按 SLOT_ORDER 取首个）；若纯藏 → 该十神 force 贡献最大的藏支槽，**force 并列按 SLOT_ORDER 升序取首**。
  - 类别：该类别下各十神主位中，按 `(transparencyFactor 降序, SLOT_ORDER 升序)` 取第一者的主位。
- `isRevealed(target): boolean` = 单神/类别在非日柱天干出现过。
- `adjacencyFactor(A, B): number`（A/B 各为 `TenGod | TenGodCategory | '日'`，**已删读 刑冲合害.贴邻 的破路径**）：
  - `'日'` 的代表槽 = `'日'`（SLOT_ORDER idx 2，恒 revealed）。
  - 若 A、B **皆非 '日' 且皆纯藏（isRevealed 皆 false）** → 0.4。
  - 否则取双方 representativeSlot 的 SLOT_ORDER idx 差 d：`d===0`(同柱)→0.8，`d===1`(贴邻)→1.0，`d>=2`(隔位)→0.5。
  - 代表槽只在 present 柱里取（time=null 不参与）。
- `bodyStrong01 = clamp01((强弱比 − WEAK_CUT) / (STRONG_CUT − WEAK_CUT))`（读 `strength.强弱比`，WEAK_CUT=0.45/STRONG_CUT=0.55 从 weights.ts）；`bodyWeak01 = 1 − bodyStrong01`。窄带 [0.45,0.55] 内连续过渡、带外饱和为 0/1 是**有意**（窄带=身强弱真正模糊区），非退化为标签 gate。
- `ramp(x, lo, hi) = clamp01((x − lo) / (hi − lo))`；`clamp01(x)=Math.max(0,Math.min(1,x))`。
- 模块私有常量集中文件顶部（照 topology.ts 范式，命名 export 便于单测）：`FORCE_REF=2.0, CTRL_REF=2.0, MIN_DEGREE=0.1, SHA_LIGHT=1.5, SHA_HEAVY=3.0, SECONDARY_RELIEF=0.6`。

- [ ] **Step 1: 写失败测试**（`test/combos.test.ts`，先只测 helper；用 `buildPillar` 造盘 + 计划2 片段）

```ts
import { describe, expect, test } from 'vitest';
import { buildPillar } from '../src/build-pillar';
import { analyzeStrength } from '../src/facts/strength';
import { analyzeRoots } from '../src/facts/roots';
import { __combosHelpers } from '../src/facts/combos';
import type { FourPillars, DayMaster, TenGodCategory } from '../src/types';

const DM_JIA: DayMaster = { gan: '甲', wuXing: '木', yinYang: '阳' };
const cats = (p: Partial<Record<TenGodCategory, number>>): Record<TenGodCategory, number> =>
  ({ 比劫: 0, 食伤: 0, 财: 0, 官杀: 0, 印: 0, ...p });

function H(four: FourPillars, dm = DM_JIA, c = cats({})) {
  const st = analyzeStrength(four, dm, c);
  const rt = analyzeRoots(four, dm);
  return __combosHelpers(four, dm, st, rt);
}

describe('combos helper', () => {
  test('force 排除日柱天干（日主自身不计比肩），日支藏干计入', () => {
    // 甲日主；日柱天干甲=日主，须排除；年/月/时干甲=比肩 计入
    const four: FourPillars = {
      年: buildPillar('甲', '甲', '子'), 月: buildPillar('甲', '乙', '丑'),
      日: buildPillar('甲', '甲', '寅'), 时: buildPillar('甲', '甲', '辰'),
    };
    const h = H(four);
    // 年干甲(比肩)+时干甲(比肩)=2.0；日干甲被排除；日支寅藏甲(本气,比肩,日支×1)=1.0；时支辰藏乙(中气劫财不计比肩)
    // 寅本气甲计入比肩 → force(比肩) 含 年/时干 2×1 + 日支寅本气甲 1.0 = 3.0
    expect(h.force('比肩')).toBeCloseTo(3.0, 5);
  });
  test('force 月支藏干 ×MONTH_BRANCH_MULT(3)', () => {
    const four: FourPillars = {
      年: buildPillar('甲', '戊', '辰'), 月: buildPillar('甲', '戊', '午'), // 午本气丁(伤官),中气己(正财)
      日: buildPillar('甲', '甲', '寅'), 时: null,
    };
    const h = H(four);
    // 午本气丁=伤官，月支×3 → force(伤官) 含 1.0×3 = 3.0（不含他处）
    expect(h.force('伤官')).toBeCloseTo(3.0, 5);
  });
  test('transparencyFactor：透根1.0 / 透无根0.7 / 藏本气0.5 / 无0', () => {
    // 甲日；庚透在年干(七杀)，年支申藏庚(本气)→庚有根→透根1.0
    const rooted: FourPillars = {
      年: buildPillar('甲', '庚', '申'), 月: buildPillar('甲', '甲', '寅'),
      日: buildPillar('甲', '甲', '寅'), 时: null,
    };
    expect(H(rooted).t('七杀')).toBeCloseTo(1.0, 5);
    // 庚透年干但全盘无金根 → 透无根0.7
    const floating: FourPillars = {
      年: buildPillar('甲', '庚', '寅'), 月: buildPillar('甲', '甲', '卯'),
      日: buildPillar('甲', '甲', '寅'), 时: null,
    };
    expect(H(floating).t('七杀')).toBeCloseTo(0.7, 5);
    // 戊只藏于寅(余气)、辰(本气)…构造仅藏本气：辰本气戊(偏财) 无透 → 0.5
    const hidden: FourPillars = {
      年: buildPillar('甲', '甲', '辰'), 月: buildPillar('甲', '甲', '卯'),
      日: buildPillar('甲', '甲', '寅'), 时: null,
    };
    expect(H(hidden).t('偏财')).toBeCloseTo(0.5, 5); // 辰本气戊=偏财，仅藏本气
    expect(H(hidden).t('正官')).toBe(0); // 辛(正官)全缺
  });
  test('adjacencyFactor：贴邻1.0/同柱0.8/隔位0.5/纯藏0.4 + 确定性', () => {
    const four: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'), // 辛年(正官) 丁月(伤官)
      日: buildPillar('甲', '甲', '寅'), 时: buildPillar('甲', '己', '未'), // 己时(正财)
    };
    const h = H(four);
    expect(h.adj('正官', '伤官')).toBeCloseTo(1.0, 5); // 年↔月 贴邻
    expect(h.adj('伤官', '正财')).toBeCloseTo(0.5, 5); // 月(idx1)↔时(idx3) 隔位
    expect(h.adj('正官', '正官')).toBeCloseTo(0.8, 5); // 同位 → 同柱
  });
  test('cp 类别走 categoryForce（不漏同类另一十神，未饱和）', () => {
    // 甲日印=水：偏印壬(藏申中气0.6)、正印癸(藏子本气1.0+辰余气0.3=1.3)；cf(印)=1.9 都 <2 不饱和
    const four: FourPillars = {
      年: buildPillar('甲', '甲', '申'), 月: buildPillar('甲', '甲', '寅'),
      日: buildPillar('甲', '甲', '子'), 时: buildPillar('甲', '甲', '辰'),
    };
    const h = H(four);
    expect(h.cp('偏印')).toBeCloseTo(0.3, 5);   // 壬 0.6 / 2
    expect(h.cp('印')).toBeGreaterThan(h.cp('偏印')); // 类别含正+偏 > 单偏印
    expect(h.cp('印')).toBeLessThan(1);         // 1.9/2=0.95 未饱和
  });
  test('bodyStrong01 连续：强弱比0.45→0, 0.50→0.5, 0.55→1', () => {
    const h = H({ 年: buildPillar('甲','甲','寅'), 月: buildPillar('甲','甲','寅'), 日: buildPillar('甲','甲','寅'), 时: null });
    expect(h.bodyStrong01(0.45)).toBeCloseTo(0, 5);
    expect(h.bodyStrong01(0.50)).toBeCloseTo(0.5, 5);
    expect(h.bodyStrong01(0.55)).toBeCloseTo(1, 5);
    expect(h.bodyStrong01(0.60)).toBeCloseTo(1, 5); // 带外饱和
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/combos.test.ts` → FAIL（`__combosHelpers` 不存在）。

- [ ] **Step 3: 实现 `combos.ts` 上半**（helper + 测试钩子 `__combosHelpers`）

```ts
import { STEM_WEIGHT, HIDDEN_WEIGHT, MONTH_BRANCH_MULT, STRONG_CUT, WEAK_CUT } from '../constants/weights';
import { category } from '../ten-gods';
import {
  SLOT_ORDER, type FourPillars, type DayMaster, type StrengthAnalysis, type StemRoots,
  type TenGod, type TenGodCategory, type PillarSlot, TEN_GODS,
} from '../types';

export const FORCE_REF = 2.0;
export const CTRL_REF = 2.0;
export const MIN_DEGREE = 0.1;
export const SHA_LIGHT = 1.5;
export const SHA_HEAVY = 3.0;
export const SECONDARY_RELIEF = 0.6;

const clamp01 = (x: number): number => Math.max(0, Math.min(1, x));
const ramp = (x: number, lo: number, hi: number): number => clamp01((x - lo) / (hi - lo));
const slotIdx = (s: PillarSlot): number => SLOT_ORDER.indexOf(s);
const isCategory = (t: TenGod | TenGodCategory): t is TenGodCategory =>
  !(TEN_GODS as readonly string[]).includes(t);

type GodsByCat = Record<TenGodCategory, TenGod[]>;
const GODS_BY_CAT: GodsByCat = (() => {
  const m: GodsByCat = { 比劫: [], 食伤: [], 财: [], 官杀: [], 印: [] };
  for (const g of TEN_GODS) m[category(g)].push(g);
  return m;
})();

export interface CombosHelpers {
  force: (g: TenGod) => number;
  categoryForce: (c: TenGodCategory) => number;
  fn: (x: number) => number;
  cp: (t: TenGod | TenGodCategory) => number;
  t: (g: TenGod) => number;
  catT: (c: TenGodCategory) => number;
  adj: (a: TenGod | TenGodCategory | '日', b: TenGod | TenGodCategory | '日') => number;
  bodyStrong01: (r: number) => number;
  bodyWeak01: (r: number) => number;
  isRevealed: (t: TenGod | TenGodCategory) => boolean;
  representativeSlot: (t: TenGod | TenGodCategory) => PillarSlot | null;
}

export function __combosHelpers(
  four: FourPillars, dm: DayMaster, strength: StrengthAnalysis, roots: StemRoots[],
): CombosHelpers {
  const present = SLOT_ORDER.filter((s) => four[s] !== null) as PillarSlot[];

  // ---- force / categoryForce（一次性算）----
  const forceByGod: Record<TenGod, number> = Object.fromEntries(TEN_GODS.map((g) => [g, 0])) as Record<TenGod, number>;
  // 每神主位候选：透出槽列表 + 藏支槽贡献
  const revealedSlots: Record<TenGod, PillarSlot[]> = Object.fromEntries(TEN_GODS.map((g) => [g, [] as PillarSlot[]])) as Record<TenGod, PillarSlot[]>;
  const hiddenContribBySlot: Record<TenGod, { slot: PillarSlot; w: number }[]> = Object.fromEntries(TEN_GODS.map((g) => [g, [] as { slot: PillarSlot; w: number }[]])) as Record<TenGod, { slot: PillarSlot; w: number }[]>;
  for (const slot of present) {
    const p = four[slot]!;
    if (slot !== '日') { forceByGod[p.ganGod] += STEM_WEIGHT; revealedSlots[p.ganGod].push(slot); }
    const mult = slot === '月' ? MONTH_BRANCH_MULT : 1.0;
    for (const h of p.hiddenStems) {
      const w = HIDDEN_WEIGHT[h.role] * mult;
      forceByGod[h.god] += w;
      hiddenContribBySlot[h.god].push({ slot, w });
    }
  }
  const force = (g: TenGod): number => forceByGod[g];
  const categoryForce = (c: TenGodCategory): number => GODS_BY_CAT[c].reduce((a, g) => a + forceByGod[g], 0);
  const fn = (x: number): number => clamp01(x / FORCE_REF);
  const cp = (t: TenGod | TenGodCategory): number =>
    clamp01((isCategory(t) ? categoryForce(t) : force(t)) / CTRL_REF);

  // ---- transparencyFactor ----
  const transparencyByGod: Record<TenGod, number> = Object.fromEntries(TEN_GODS.map((g) => [g, 0])) as Record<TenGod, number>;
  for (const g of TEN_GODS) {
    const rs = revealedSlots[g];
    if (rs.length > 0) {
      const rooted = rs.some((slot) => {
        const stem = four[slot]!.gan;
        const sr = roots.find((r) => r.slot === slot && r.stem === stem);
        return sr ? sr.hasRoot : false;
      });
      transparencyByGod[g] = rooted ? 1.0 : 0.7;
    } else {
      let benqi = false, zhongyu = false;
      for (const slot of present) {
        for (const h of four[slot]!.hiddenStems) {
          if (h.god === g) { if (h.role === '本气') benqi = true; else zhongyu = true; }
        }
      }
      transparencyByGod[g] = benqi ? 0.5 : zhongyu ? 0.3 : 0;
    }
  }
  const t = (g: TenGod): number => transparencyByGod[g];
  const catT = (c: TenGodCategory): number => Math.max(0, ...GODS_BY_CAT[c].map((g) => transparencyByGod[g]));

  const isRevealedGod = (g: TenGod): boolean => revealedSlots[g].length > 0;
  const isRevealed = (tg: TenGod | TenGodCategory): boolean =>
    isCategory(tg) ? GODS_BY_CAT[tg].some(isRevealedGod) : isRevealedGod(tg);

  const repSlotGod = (g: TenGod): PillarSlot | null => {
    if (revealedSlots[g].length > 0) {
      return [...revealedSlots[g]].sort((a, b) => slotIdx(a) - slotIdx(b))[0]!;
    }
    const hc = hiddenContribBySlot[g];
    if (hc.length === 0) return null;
    let best = hc[0]!;
    for (const c of hc) if (c.w > best.w || (c.w === best.w && slotIdx(c.slot) < slotIdx(best.slot))) best = c;
    return best.slot;
  };
  const representativeSlot = (tg: TenGod | TenGodCategory): PillarSlot | null => {
    if (!isCategory(tg)) return repSlotGod(tg);
    const gods = GODS_BY_CAT[tg].filter((g) => repSlotGod(g) !== null);
    if (gods.length === 0) return null;
    gods.sort((a, b) => transparencyByGod[b] - transparencyByGod[a] || slotIdx(repSlotGod(a)!) - slotIdx(repSlotGod(b)!));
    return repSlotGod(gods[0]!);
  };

  const adj = (a: TenGod | TenGodCategory | '日', b: TenGod | TenGodCategory | '日'): number => {
    const slotOf = (x: typeof a): PillarSlot | null => (x === '日' ? '日' : representativeSlot(x));
    const sa = slotOf(a), sb = slotOf(b);
    if (sa === null || sb === null) return 0;
    const aHidden = a !== '日' && !isRevealed(a);
    const bHidden = b !== '日' && !isRevealed(b);
    if (aHidden && bHidden) return 0.4;
    const d = Math.abs(slotIdx(sa) - slotIdx(sb));
    return d === 0 ? 0.8 : d === 1 ? 1.0 : 0.5;
  };

  const bodyStrong01 = (r: number): number => clamp01((r - WEAK_CUT) / (STRONG_CUT - WEAK_CUT));
  const bodyWeak01 = (r: number): number => 1 - bodyStrong01(r);

  return { force, categoryForce, fn, cp, t, catT, adj, bodyStrong01, bodyWeak01, isRevealed, representativeSlot };
}
```

> 注：`TEN_GODS` 需从 types.ts 正常 export（已 export，见 types.ts 第 5 行）。`StemRoots` 类型也已 export。

- [ ] **Step 4: 跑绿** — `npx vitest run test/combos.test.ts && npx tsc --noEmit` → PASS。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/facts/combos.ts bazi-core/test/combos.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: combos 共享 helper(force/cp/透藏/相邻/身强弱连续门·抗查表·确定性)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: `detectGodCombos` — 12 组合（`combos.ts` 下半）

**12 组合精确公式**（degree = gate × 透藏 × 旺衰 × 相邻 × 制化；`cf`=categoryForce；类别项 `fn(cf(...))`；制化 cp 已按类别/单神分派）：

| # | 组合 | 极性 | 内含矛盾 | gate | 透藏 | 旺衰 | 相邻 | 制化 |
|---|---|---|---|---|---|---|---|---|
|1|伤官见官|凶|是|force(伤官)>0且force(正官)>0|min(t伤官,t正官)|min(fn(f伤官),fn(f正官))|adj(伤官,正官)|1−cp(印)|
|2|伤官配印|吉|否|force(伤官)>0且cf(印)>0|min(t伤官,catT印)|min(fn(f伤官),fn(cf印))|adj(伤官,印)|1−cp(财)|
|3|食神制杀|吉|否|force(食神)>0且force(七杀)>0|min(t食神,t七杀)|min(fn(f食神),fn(f七杀))|adj(食神,七杀)|1−cp(偏印)×(1−cp(财))|
|4|财滋弱杀|凶|否|cf(财)>0且force(七杀)>0|min(catT财,t七杀)|min(fn(cf财),fn(f七杀))×bodyWeak01|adj(财,七杀)|1−cp(印)|
|5|财多身弱|凶|是|cf(财)>0|catT财|fn(cf财)×bodyWeak01|adj(财,日)|1−cp(印)×(1−cp(财))|
|6|印重身旺|凶|是|cf(印)>0|catT印|fn(cf印)×bodyStrong01|adj(印,日)|1−max(cp(财),cp(食伤)×(1−cp(印)))|
|7|杀重无制|凶|是|force(七杀)>0|t七杀|ramp(force(七杀),SHA_LIGHT,SHA_HEAVY)|adj(七杀,日)|(1−cp(食伤))×(1−cp(印))|
|8|杀印相生|吉|否|force(七杀)>0且cf(印)>0|min(t七杀,catT印)|min(fn(f七杀),fn(cf印))|adj(七杀,印)|1−max(cp(财),cp(食伤)×(1−cp(印)))|
|9|食伤生财|吉|否|cf(食伤)>0且cf(财)>0|min(catT食伤,catT财)|min(fn(cf食伤),fn(cf财))|adj(食伤,财)|1−cp(印)|
|10|比劫夺财|凶|否|cf(比劫)>0且cf(财)>0|min(catT比劫,catT财)|min(fn(cf比劫),fn(cf财))×bodyStrong01|adj(比劫,财)|1−max(cp(官杀),SECONDARY_RELIEF×cp(食伤))|
|11|官印相生|吉|否|force(正官)>0且cf(印)>0|min(t正官,catT印)|min(fn(f正官),fn(cf印))|adj(正官,印)|1−degree原值(伤官见官)|
|12|伤官生财|吉|否|force(伤官)>0且cf(财)>0|min(t伤官,catT财)|min(fn(f伤官),fn(cf财))|adj(伤官,财)|1−cp(印)|

**铁律：**
- **计算顺序：#1 伤官见官 必先算，缓存其 raw degree（round 与 MIN_DEGREE 过滤之前的全精度值）**。#11 制化项读此缓存（即使 #1 因 <MIN_DEGREE 不进最终输出，缓存照用）。
- gate 为 0 → degree=0（gate 写成 `(在场?1:0)` 乘子）。
- 每 hit 的 `力量快照` 全精度，含核心十神 force + `'制化系数'`(该组合制化维最终乘子) + `'原始成立度'`(raw degree)。
- 输出：`成立程度=round2(raw)`；过滤 `raw < MIN_DEGREE` 丢弃；按 `成立程度` 降序（并列按 GodComboKind 在 `COMBO_ORDER` 中的固定序，确定性）。

- [ ] **Step 1: 写失败测试**（追加到 `test/combos.test.ts`）

```ts
import { detectGodCombos } from '../src/facts/combos';
import type { WuXing } from '../src/types';

function combos(four: FourPillars, dm = DM_JIA, c = cats({})) {
  const st = analyzeStrength(four, dm, c);
  const rt = analyzeRoots(four, dm);
  return detectGodCombos(four, dm, st, rt);
}
const find = (cs: ReturnType<typeof combos>, k: string) => cs.find((x) => x.类型 === k);

describe('detectGodCombos 12 组合', () => {
  test('伤官见官：伤官+正官同透贴邻无印 → 凶向命中、内含矛盾', () => {
    // 甲日；丁(伤官)月干、辛(正官)年干、贴邻；无水印
    const four: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'),
      日: buildPillar('甲', '甲', '午'), 时: buildPillar('甲', '丁', '未'),
    };
    const h = find(combos(four), '伤官见官');
    expect(h).toBeDefined();
    expect(h!.极性).toBe('凶向');
    expect(h!.内含矛盾).toBe(true);
    expect(h!.成立程度).toBeGreaterThan(0.1);
  });
  test('印化伤：同盘加重水印 → 伤官见官被压低、伤官配印抬升', () => {
    const noYin: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'),
      日: buildPillar('甲', '甲', '午'), 时: buildPillar('甲', '丁', '未'),
    };
    const withYin: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'),
      日: buildPillar('甲', '甲', '子'), 时: buildPillar('甲', '癸', '亥'), // 加水印（癸+亥子）
    };
    const d0 = find(combos(noYin), '伤官见官')!.成立程度;
    const d1 = find(combos(withYin), '伤官见官')?.成立程度 ?? 0;
    expect(d1).toBeLessThan(d0); // 印化伤 → 伤官见官 degree 下降（制化第三方调制=抗查表卖点）
    expect(find(combos(withYin), '伤官配印')).toBeDefined();
  });
  test('同一七杀因制化神不同 degree 不同（抗查表卖点）', () => {
    // 食神制杀：丙(食神)制庚(七杀)
    const zhiSha: FourPillars = {
      年: buildPillar('甲', '庚', '申'), 月: buildPillar('甲', '丙', '寅'),
      日: buildPillar('甲', '甲', '寅'), 时: buildPillar('甲', '丙', '午'),
    };
    expect(find(combos(zhiSha), '食神制杀')).toBeDefined();
    // 杀重无制：庚旺(申+酉)无食伤无印
    const wuZhi: FourPillars = {
      年: buildPillar('甲', '庚', '申'), 月: buildPillar('甲', '庚', '申'),
      日: buildPillar('甲', '甲', '戌'), 时: buildPillar('甲', '辛', '酉'),
    };
    expect(find(combos(wuZhi), '杀重无制')).toBeDefined();
    expect(find(combos(wuZhi), '食神制杀')).toBeUndefined(); // 无食神
  });
  test('杀重无制：force(七杀)≈SHA_LIGHT 附近 degree≈0（ramp 连续门，抗踩线）', () => {
    // 轻杀：仅时干一庚(七杀,无根) force≈1.0 < 1.5 → ramp=0 → 不命中
    const light: FourPillars = {
      年: buildPillar('甲', '甲', '寅'), 月: buildPillar('甲', '甲', '寅'),
      日: buildPillar('甲', '甲', '寅'), 时: buildPillar('甲', '庚', '午'),
    };
    expect(find(combos(light), '杀重无制')).toBeUndefined();
  });
  test('官印相生：受伤官见官抑制（#11 读 #1 raw degree）', () => {
    // 正官+印+伤官同盘：官印相生 degree 应被伤官见官抑制（不为满）
    const four: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'), // 辛正官 丁伤官
      日: buildPillar('甲', '甲', '子'), 时: buildPillar('甲', '癸', '亥'), // 癸印
    };
    const cs = combos(four);
    const gy = find(cs, '官印相生');
    if (gy) expect(gy.力量快照['制化系数']).toBeLessThan(1); // 1−degree(伤官见官) < 1
  });
  test('确定性：同输入两次深相等', () => {
    const four: FourPillars = {
      年: buildPillar('甲', '庚', '申'), 月: buildPillar('甲', '丙', '寅'),
      日: buildPillar('甲', '甲', '辰'), 时: buildPillar('甲', '己', '丑'),
    };
    expect(combos(four)).toEqual(combos(four));
  });
  test('抗查表回归：扰动非 gate 输入，命中组合 degree 必变（不是二元常数）', () => {
    const base: FourPillars = {
      年: buildPillar('甲', '辛', '酉'), 月: buildPillar('甲', '丁', '巳'),
      日: buildPillar('甲', '甲', '午'), 时: buildPillar('甲', '丁', '未'),
    };
    const more: FourPillars = { ...base, 时: buildPillar('甲', '丁', '巳') }; // 加重伤官
    const d0 = find(combos(base), '伤官见官')!.成立程度;
    const d1 = find(combos(more), '伤官见官')!.成立程度;
    expect(d1).not.toBe(d0); // degree 随力量连续变化
  });
  test('空时柱不崩 + 全比劫盘无财类组合', () => {
    const noHour: FourPillars = {
      年: buildPillar('甲', '甲', '寅'), 月: buildPillar('甲', '甲', '寅'),
      日: buildPillar('甲', '甲', '寅'), 时: null,
    };
    expect(() => combos(noHour)).not.toThrow();
    expect(find(combos(noHour), '比劫夺财')).toBeUndefined(); // 无财
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/combos.test.ts` → FAIL（`detectGodCombos` 未定义）。

- [ ] **Step 3: 实现 `detectGodCombos`**（接 `combos.ts`）

```ts
import type { ComboHit, ComboMember, ComboPolarity, GodComboKind } from '../types';

const round2 = (x: number): number => Math.round(x * 100) / 100;

const COMBO_ORDER: GodComboKind[] = [
  '伤官见官', '伤官配印', '食神制杀', '财滋弱杀', '财多身弱', '印重身旺',
  '杀重无制', '杀印相生', '食伤生财', '比劫夺财', '官印相生', '伤官生财',
];
const NEI_HAN_MAODUN = new Set<GodComboKind>(['伤官见官', '财多身弱', '杀重无制', '印重身旺']);
const POLARITY: Record<GodComboKind, ComboPolarity> = {
  伤官见官: '凶向', 财滋弱杀: '凶向', 财多身弱: '凶向', 印重身旺: '凶向', 杀重无制: '凶向', 比劫夺财: '凶向',
  伤官配印: '吉向', 食神制杀: '吉向', 杀印相生: '吉向', 食伤生财: '吉向', 官印相生: '吉向', 伤官生财: '吉向',
};

export function detectGodCombos(
  four: FourPillars, dm: DayMaster, strength: StrengthAnalysis, roots: StemRoots[],
): ComboHit[] {
  const H = __combosHelpers(four, dm, strength, roots);
  const { force, categoryForce: cf, fn, cp, t, catT, adj, bodyStrong01, bodyWeak01 } = H;
  const r = strength.强弱比;
  const bw = bodyWeak01(r), bs = bodyStrong01(r);
  const gate = (b: boolean): number => (b ? 1 : 0);

  // 中间 raw degree + 制化系数 缓存（含被过滤者，供 #11）
  const raw: Partial<Record<GodComboKind, number>> = {};
  const zh: Partial<Record<GodComboKind, number>> = {};   // 制化系数
  const snap: Partial<Record<GodComboKind, Record<string, number>>> = {};
  const mem: Partial<Record<GodComboKind, ComboMember[]>> = {};

  const put = (k: GodComboKind, gateB: boolean, 透藏: number, 旺衰: number, 相邻: number, 制化: number,
               members: ComboMember[], extraSnap: Record<string, number> = {}): void => {
    const g = gate(gateB);
    const d = g * 透藏 * 旺衰 * 相邻 * 制化;
    raw[k] = d; zh[k] = 制化; mem[k] = members;
    snap[k] = { 制化系数: 制化, 原始成立度: d, ...extraSnap };
  };
  const M = (十神: TenGod | TenGodCategory, f: number, tr: number): ComboMember => ({ 十神, force: f, transparency: tr });

  // #1 伤官见官（必先算）
  put('伤官见官', force('伤官') > 0 && force('正官') > 0,
    Math.min(t('伤官'), t('正官')), Math.min(fn(force('伤官')), fn(force('正官'))), adj('伤官', '正官'),
    1 - cp('印'),
    [M('伤官', force('伤官'), t('伤官')), M('正官', force('正官'), t('正官'))]);
  // #2 伤官配印
  put('伤官配印', force('伤官') > 0 && cf('印') > 0,
    Math.min(t('伤官'), catT('印')), Math.min(fn(force('伤官')), fn(cf('印'))), adj('伤官', '印'),
    1 - cp('财'),
    [M('伤官', force('伤官'), t('伤官')), M('印', cf('印'), catT('印'))]);
  // #3 食神制杀
  put('食神制杀', force('食神') > 0 && force('七杀') > 0,
    Math.min(t('食神'), t('七杀')), Math.min(fn(force('食神')), fn(force('七杀'))), adj('食神', '七杀'),
    1 - cp('偏印') * (1 - cp('财')),
    [M('食神', force('食神'), t('食神')), M('七杀', force('七杀'), t('七杀'))]);
  // #4 财滋弱杀
  put('财滋弱杀', cf('财') > 0 && force('七杀') > 0,
    Math.min(catT('财'), t('七杀')), Math.min(fn(cf('财')), fn(force('七杀'))) * bw, adj('财', '七杀'),
    1 - cp('印'),
    [M('财', cf('财'), catT('财')), M('七杀', force('七杀'), t('七杀'))], { bodyWeak01: bw });
  // #5 财多身弱
  put('财多身弱', cf('财') > 0,
    catT('财'), fn(cf('财')) * bw, adj('财', '日'),
    1 - cp('印') * (1 - cp('财')),
    [M('财', cf('财'), catT('财'))], { bodyWeak01: bw });
  // #6 印重身旺
  put('印重身旺', cf('印') > 0,
    catT('印'), fn(cf('印')) * bs, adj('印', '日'),
    1 - Math.max(cp('财'), cp('食伤') * (1 - cp('印'))),
    [M('印', cf('印'), catT('印'))], { bodyStrong01: bs });
  // #7 杀重无制
  put('杀重无制', force('七杀') > 0,
    t('七杀'), ramp(force('七杀'), SHA_LIGHT, SHA_HEAVY), adj('七杀', '日'),
    (1 - cp('食伤')) * (1 - cp('印')),
    [M('七杀', force('七杀'), t('七杀'))]);
  // #8 杀印相生
  put('杀印相生', force('七杀') > 0 && cf('印') > 0,
    Math.min(t('七杀'), catT('印')), Math.min(fn(force('七杀')), fn(cf('印'))), adj('七杀', '印'),
    1 - Math.max(cp('财'), cp('食伤') * (1 - cp('印'))),
    [M('七杀', force('七杀'), t('七杀')), M('印', cf('印'), catT('印'))]);
  // #9 食伤生财
  put('食伤生财', cf('食伤') > 0 && cf('财') > 0,
    Math.min(catT('食伤'), catT('财')), Math.min(fn(cf('食伤')), fn(cf('财'))), adj('食伤', '财'),
    1 - cp('印'),
    [M('食伤', cf('食伤'), catT('食伤')), M('财', cf('财'), catT('财'))]);
  // #10 比劫夺财
  put('比劫夺财', cf('比劫') > 0 && cf('财') > 0,
    Math.min(catT('比劫'), catT('财')), Math.min(fn(cf('比劫')), fn(cf('财'))) * bs, adj('比劫', '财'),
    1 - Math.max(cp('官杀'), SECONDARY_RELIEF * cp('食伤')),
    [M('比劫', cf('比劫'), catT('比劫')), M('财', cf('财'), catT('财'))], { bodyStrong01: bs });
  // #11 官印相生（读 #1 raw degree）
  put('官印相生', force('正官') > 0 && cf('印') > 0,
    Math.min(t('正官'), catT('印')), Math.min(fn(force('正官')), fn(cf('印'))), adj('正官', '印'),
    1 - (raw['伤官见官'] ?? 0),
    [M('正官', force('正官'), t('正官')), M('印', cf('印'), catT('印'))]);
  // #12 伤官生财
  put('伤官生财', force('伤官') > 0 && cf('财') > 0,
    Math.min(t('伤官'), catT('财')), Math.min(fn(force('伤官')), fn(cf('财'))), adj('伤官', '财'),
    1 - cp('印'),
    [M('伤官', force('伤官'), t('伤官')), M('财', cf('财'), catT('财'))]);

  const hits: ComboHit[] = COMBO_ORDER
    .filter((k) => (raw[k] ?? 0) >= MIN_DEGREE)
    .map((k) => ({
      类型: k, 成立程度: round2(raw[k]!), 极性: POLARITY[k], 内含矛盾: NEI_HAN_MAODUN.has(k),
      members: mem[k]!, 力量快照: snap[k]!,
      说明键: `${k}·${mem[k]!.map((m) => `${m.十神}${round2(m.force)}`).join('·')}·制化${round2(zh[k]!)}`,
    }))
    .sort((a, b) => b.成立程度 - a.成立程度 || COMBO_ORDER.indexOf(a.类型) - COMBO_ORDER.indexOf(b.类型));
  return hits;
}
```

- [ ] **Step 4: 跑绿** — `npx vitest run test/combos.test.ts && npx tsc --noEmit` → PASS。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/facts/combos.ts bazi-core/test/combos.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: detectGodCombos 12 组合(连续成立度×制化第三方·抗查表·伤官见官先于官印相生)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: `tension.ts` — 矛盾张力轴（派生层，读 combos/clashes 不重算）

**Files:** Create `bazi-core/src/facts/tension.ts`；Test `bazi-core/test/tension.test.ts`

**精确语义：**
- 模块私有常量：`AXIS_MIN_DEGREE=0.3, AXIS_RELIEF=0.6, AXIS_KEEP_MIN=0.2, RELIEF_GATE=0.7`。
- **X1 相战对**：遍历 `topology.clashes`；`catA=categoryOf(clash.克,dmW)`，`catB=categoryOf(clash.受,dmW)`；按 `[catA,catB]` 经 `CAT_ORDER` 规范化排序后查 `PAIR_AXIS`；命中 → `{轴名,来源类型:'相战',来源cite:'${克}${受}相战',两极,张力强度:clash.intensity,优缺同源:false}`。
- **X2 内含矛盾派生**：combo `内含矛盾===true && 成立程度>AXIS_MIN_DEGREE`；查 `MAODUN_AXIS`（4 条）；`有制化 = 力量快照['制化系数'] <= RELIEF_GATE`；`张力强度=成立程度×(有制化?AXIS_RELIEF:1.0)`；`优缺同源=true,来源类型:'内含矛盾',来源cite:combo.类型`。
- **X2 反向共存**：输出 combos 同含 `杀印相生`+`伤官见官` → 元张力 `{轴名:'化解vs冲突',来源类型:'元张力',来源cite:'杀印相生+伤官见官',两极:['疏导整合','内在冲突'],张力强度:min(二者成立程度),优缺同源:false}`。
- **X3 优缺同源点**：X2 优缺同源轴 → `{结构cite,同一来源,正面表述:两极[0],负面表述:两极[1]}`。
- **X4 去重排序**：同 `轴名` 取 max 强度；降序（并列按轴名 localeCompare 确定）；丢 `<AXIS_KEEP_MIN`；输出 round2 张力强度。

`PAIR_AXIS`（键=类别对按 `CAT_ORDER=['比劫','食伤','财','官杀','印']` 排序后 join '×'）：
`比劫×财→占有vs分享[占有,分享]` / `食伤×官杀→权威vs才华[权威,才华]` / `财×印→守成vs进取[守成,进取]` / `比劫×官杀→自主vs受制[自主,受制]` / `食伤×印→表达vs内省[表达,内省]`。
`MAODUN_AXIS`：`伤官见官→才华锋芒vs规则权威[恃才放旷,守序服从]` / `财多身弱→欲望vs承载力[贪取扩张,力不能及]` / `杀重无制→压力/野心vs失控[进取担当,被压垮]` / `印重身旺→庇护vs依赖[厚学多受,迟滞被动]`。

- [ ] **Step 1: 写失败测试**（`test/tension.test.ts`）

```ts
import { describe, expect, test } from 'vitest';
import { buildTension } from '../src/facts/tension';
import type { ComboHit, EmergentTopology, WuXing } from '../src/types';

const emptyTopo = (clashes: EmergentTopology['clashes'] = []): EmergentTopology => ({
  flow: 0, flowLevel: '阻', coverage: 0, chainStrength: 0, breaks: [],
  isFullCycle: false, cyclePresent: [], longestChain: { path: [], len: 0 },
  clashes, tongGuanReport: [], dominant: [], missing: [], presentRows: [], caveats: [],
});
const combo = (类型: ComboHit['类型'], 成立程度: number, 内含矛盾: boolean, 制化系数 = 1): ComboHit => ({
  类型, 成立程度, 极性: 内含矛盾 ? '凶向' : '吉向', 内含矛盾, members: [],
  力量快照: { 制化系数, 原始成立度: 成立程度 }, 说明键: '',
});

describe('buildTension', () => {
  test('X1 相战对 → 查轴（甲日木土相战=占有vs分享）', () => {
    const topo = emptyTopo([{ pair: ['木', '土'], 克: '木', 受: '土', scores: [3, 3], tongGuanShen: '火', tongGuanPresent: false, tongGuanScore: 0, intensity: 0.8, detail: '' }]);
    const r = buildTension(topo, [], '木');
    const ax = r.矛盾张力轴.find((a) => a.轴名 === '占有vs分享');
    expect(ax).toBeDefined();
    expect(ax!.张力强度).toBeCloseTo(0.8, 5);
    expect(ax!.优缺同源).toBe(false);
  });
  test('X2 内含矛盾派生 + 优缺同源点（伤官见官，有制化打折）', () => {
    const r = buildTension(emptyTopo(), [combo('伤官见官', 0.8, true, 0.5)], '木');
    const ax = r.矛盾张力轴.find((a) => a.轴名 === '才华锋芒vs规则权威');
    expect(ax).toBeDefined();
    expect(ax!.优缺同源).toBe(true);
    expect(ax!.张力强度).toBeCloseTo(0.48, 5); // 0.8×0.6（制化系数0.5<=0.7=有制化）
    expect(r.优缺同源点.some((p) => p.正面表述 === '恃才放旷')).toBe(true);
  });
  test('X2 无制化不打折', () => {
    const r = buildTension(emptyTopo(), [combo('杀重无制', 0.5, true, 0.9)], '木');
    const ax = r.矛盾张力轴.find((a) => a.轴名 === '压力/野心vs失控');
    expect(ax!.张力强度).toBeCloseTo(0.5, 5); // 制化系数0.9>0.7 → 无制化 → ×1
  });
  test('X4 丢弱轴 + 低于阈值不出', () => {
    const r = buildTension(emptyTopo(), [combo('财多身弱', 0.35, true, 1)], '木');
    // 0.35>AXIS_MIN_DEGREE(0.3) 进 X2，强度 0.35>AXIS_KEEP_MIN(0.2) 保留
    expect(r.矛盾张力轴.find((a) => a.轴名 === '欲望vs承载力')).toBeDefined();
    const r2 = buildTension(emptyTopo(), [combo('财多身弱', 0.25, true, 1)], '木');
    expect(r2.矛盾张力轴.find((a) => a.轴名 === '欲望vs承载力')).toBeUndefined(); // 0.25<0.3 不进 X2
  });
  test('反向共存元张力', () => {
    const r = buildTension(emptyTopo(), [combo('杀印相生', 0.6, false), combo('伤官见官', 0.5, true, 0.9)], '木');
    expect(r.矛盾张力轴.find((a) => a.轴名 === '化解vs冲突')).toBeDefined();
  });
  test('确定性', () => {
    const cs = [combo('伤官见官', 0.8, true, 0.5)];
    expect(buildTension(emptyTopo(), cs, '木')).toEqual(buildTension(emptyTopo(), cs, '木'));
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/tension.test.ts` → FAIL。

- [ ] **Step 3: 实现 `tension.ts`**

```ts
import { categoryOf } from '../ten-gods';
import type {
  EmergentTopology, ComboHit, TensionResult, TensionAxis, SameSourcePoint,
  TenGodCategory, GodComboKind, WuXing,
} from '../types';

const AXIS_MIN_DEGREE = 0.3;
const AXIS_RELIEF = 0.6;
const AXIS_KEEP_MIN = 0.2;
const RELIEF_GATE = 0.7;
const round2 = (x: number): number => Math.round(x * 100) / 100;

const CAT_ORDER: TenGodCategory[] = ['比劫', '食伤', '财', '官杀', '印'];
const pairKey = (a: TenGodCategory, b: TenGodCategory): string =>
  [a, b].sort((x, y) => CAT_ORDER.indexOf(x) - CAT_ORDER.indexOf(y)).join('×');
const PAIR_AXIS: Record<string, { 轴名: string; 两极: [string, string] }> = {
  [pairKey('财', '比劫')]: { 轴名: '占有vs分享', 两极: ['占有', '分享'] },
  [pairKey('官杀', '食伤')]: { 轴名: '权威vs才华', 两极: ['权威', '才华'] },
  [pairKey('印', '财')]: { 轴名: '守成vs进取', 两极: ['守成', '进取'] },
  [pairKey('比劫', '官杀')]: { 轴名: '自主vs受制', 两极: ['自主', '受制'] },
  [pairKey('食伤', '印')]: { 轴名: '表达vs内省', 两极: ['表达', '内省'] },
};
const MAODUN_AXIS: Partial<Record<GodComboKind, { 轴名: string; 两极: [string, string] }>> = {
  伤官见官: { 轴名: '才华锋芒vs规则权威', 两极: ['恃才放旷', '守序服从'] },
  财多身弱: { 轴名: '欲望vs承载力', 两极: ['贪取扩张', '力不能及'] },
  杀重无制: { 轴名: '压力/野心vs失控', 两极: ['进取担当', '被压垮'] },
  印重身旺: { 轴名: '庇护vs依赖', 两极: ['厚学多受', '迟滞被动'] },
};

export function buildTension(topology: EmergentTopology, combos: ComboHit[], dmW: WuXing): TensionResult {
  const axes: TensionAxis[] = [];
  const 优缺同源点: SameSourcePoint[] = [];

  // X1
  for (const c of topology.clashes) {
    const a = categoryOf(c.克, dmW), b = categoryOf(c.受, dmW);
    const def = PAIR_AXIS[pairKey(a, b)];
    if (def) axes.push({ 轴名: def.轴名, 来源类型: '相战', 来源cite: `${c.克}${c.受}相战`, 两极: def.两极, 张力强度: c.intensity, 优缺同源: false });
  }
  // X2 内含矛盾
  for (const c of combos) {
    if (!c.内含矛盾 || c.成立程度 <= AXIS_MIN_DEGREE) continue;
    const def = MAODUN_AXIS[c.类型];
    if (!def) continue;
    const 有制化 = (c.力量快照['制化系数'] ?? 1) <= RELIEF_GATE;
    const 张力强度 = c.成立程度 * (有制化 ? AXIS_RELIEF : 1.0);
    axes.push({ 轴名: def.轴名, 来源类型: '内含矛盾', 来源cite: c.类型, 两极: def.两极, 张力强度, 优缺同源: true });
    优缺同源点.push({ 结构cite: c.类型, 同一来源: c.类型, 正面表述: def.两极[0], 负面表述: def.两极[1] });
  }
  // X2 反向共存
  const sps = combos.find((c) => c.类型 === '杀印相生');
  const sgjg = combos.find((c) => c.类型 === '伤官见官');
  if (sps && sgjg) {
    axes.push({ 轴名: '化解vs冲突', 来源类型: '元张力', 来源cite: '杀印相生+伤官见官', 两极: ['疏导整合', '内在冲突'], 张力强度: Math.min(sps.成立程度, sgjg.成立程度), 优缺同源: false });
  }
  // X4 去重(同名取 max) + 排序 + 丢弱
  const byName = new Map<string, TensionAxis>();
  for (const ax of axes) {
    const cur = byName.get(ax.轴名);
    if (!cur || ax.张力强度 > cur.张力强度) byName.set(ax.轴名, ax);
  }
  const 矛盾张力轴 = [...byName.values()]
    .filter((ax) => ax.张力强度 >= AXIS_KEEP_MIN)
    .sort((a, b) => b.张力强度 - a.张力强度 || a.轴名.localeCompare(b.轴名))
    .map((ax) => ({ ...ax, 张力强度: round2(ax.张力强度) }));
  return { 矛盾张力轴, 优缺同源点 };
}
```

- [ ] **Step 4: 跑绿** — `npx vitest run test/tension.test.ts && npx tsc --noEmit` → PASS。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/facts/tension.ts bazi-core/test/tension.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: buildTension 矛盾张力轴(相战派生/内含矛盾优缺同源/反向共存元张力·读 combos 不重算)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: `pattern.ts` — 格局机械立格

**Files:** Create `bazi-core/src/facts/pattern.ts`；Test `bazi-core/test/pattern.test.ts`

**精确语义：**
- `monthGod=tenGod(dm.gan, HIDDEN_STEMS[月支][0].gan)`（月支本气十神）；`cat=category(monthGod)`。
- **禄刃**（`cat==='比劫'`）：`比肩→建禄格(依据'建禄')`；`劫财→` 阳日(`GAN_YINYANG[dm.gan]==='阳'`)`羊刃格(依据'羊刃')` 否则 `月劫格(依据'月劫')`；`透干=false`。
- **八正格**（其余 cat）：透干集 = 年/月/时天干（排除日）的 ganGod。①`monthGod` 在透干集 → `格=GE_NAME_BY_GOD[monthGod],依据'本气透干',透干=true`；②否则按 `HIDDEN_STEMS[月支]` 从 idx1（中气→余气）取首个其 god 在透干集者 → `格=GE_NAME_BY_GOD[那god],依据'杂气透干',透干=true`；③全不透 → `格=GE_NAME_BY_GOD[monthGod],依据'月令本气暗藏未透',透干=false`。
- **备选格**：月支各藏干中，god 在透干集 且 其格名≠主格 → 收 `GE_NAME_BY_GOD[god]`，dedup。
- `GE_NAME_BY_GOD`：食神格/伤官格/正财格/偏财格/正官格/七杀格/正印格/偏印格（比肩/劫财 走禄刃，不入此表）。**不 judge 成格破格高低清浊。**

- [ ] **Step 1: 写失败测试**（`test/pattern.test.ts`）

```ts
import { describe, expect, test } from 'vitest';
import { buildPillar } from '../src/build-pillar';
import { buildPattern } from '../src/facts/pattern';
import type { FourPillars, DayMaster } from '../src/types';

const DM = (gan: '甲'|'乙'|'丙'|'丁', w: '木'|'火', yy: '阳'|'阴'): DayMaster => ({ gan, wuXing: w, yinYang: yy });
const four = (yg: string, mg: string, mz: string, dz = '寅'): FourPillars => ({
  年: buildPillar('甲', yg as any, '子'), 月: buildPillar('甲', mg as any, mz as any),
  日: buildPillar('甲', '甲', dz as any), 时: null,
});

describe('buildPattern 机械立格', () => {
  test('羊刃：甲日卯月(本气乙=劫财,阳日)→羊刃格', () => {
    const p = buildPattern(DM('甲','木','阳'), four('甲','甲','卯'));
    expect(p.格).toBe('羊刃格'); expect(p.立格依据).toBe('羊刃');
  });
  test('建禄：乙日卯月(本气乙=比肩)→建禄格', () => {
    const f: FourPillars = { 年: buildPillar('乙','乙','子'), 月: buildPillar('乙','乙','卯'), 日: buildPillar('乙','乙','卯'), 时: null };
    const p = buildPattern({ gan:'乙', wuXing:'木', yinYang:'阴' }, f);
    expect(p.格).toBe('建禄格'); expect(p.立格依据).toBe('建禄');
  });
  test('本气透干：甲日酉月(本气辛=正官)且辛透年干→正官格,本气透干', () => {
    const p = buildPattern(DM('甲','木','阳'), four('辛','甲','酉'));
    expect(p.格).toBe('正官格'); expect(p.立格依据).toBe('本气透干'); expect(p.透干).toBe(true);
  });
  test('本气暗藏未透：甲日酉月(辛)无辛透→正官格,暗藏未透,透干 false', () => {
    const p = buildPattern(DM('甲','木','阳'), four('甲','甲','酉'));
    expect(p.格).toBe('正官格'); expect(p.立格依据).toBe('月令本气暗藏未透'); expect(p.透干).toBe(false);
  });
  test('杂气透干：甲日辰月(本气戊=偏财不透)、中气乙=劫财不算八正、余气癸=正印透时干→正印格,杂气透干', () => {
    const f: FourPillars = { 年: buildPillar('甲','甲','子'), 月: buildPillar('甲','甲','辰'), 日: buildPillar('甲','甲','寅'), 时: buildPillar('甲','癸','亥') };
    const p = buildPattern(DM('甲','木','阳'), f);
    expect(p.立格依据).toBe('杂气透干'); expect(p.透干).toBe(true);
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/pattern.test.ts` → FAIL。

- [ ] **Step 3: 实现 `pattern.ts`**

```ts
import { GAN_YINYANG, HIDDEN_STEMS } from '../constants/gan-zhi';
import { tenGod, category } from '../ten-gods';
import { SLOT_ORDER, type FourPillars, type DayMaster, type PatternResult, type GeName, type TenGod, type PillarSlot } from '../types';

const GE_NAME_BY_GOD: Partial<Record<TenGod, GeName>> = {
  食神: '食神格', 伤官: '伤官格', 偏财: '偏财格', 正财: '正财格',
  七杀: '七杀格', 正官: '正官格', 偏印: '偏印格', 正印: '正印格',
};

export function buildPattern(dm: DayMaster, four: FourPillars): PatternResult {
  const monthZhi = four.月!.zhi;
  const monthBenQi = HIDDEN_STEMS[monthZhi][0]!.gan;
  const monthGod = tenGod(dm.gan, monthBenQi);
  const cat = category(monthGod);

  // 禄刃
  if (cat === '比劫') {
    if (monthGod === '比肩') return { 格: '建禄格', 立格依据: '建禄', 透干: false, 月令本气十神: monthGod, 备选格: [], 说明: `月令${monthZhi}本气${monthBenQi}为日主同类(建禄)` };
    const isYang = GAN_YINYANG[dm.gan] === '阳';
    const 格: GeName = isYang ? '羊刃格' : '月劫格';
    return { 格, 立格依据: isYang ? '羊刃' : '月劫', 透干: false, 月令本气十神: monthGod, 备选格: [], 说明: `月令${monthZhi}本气${monthBenQi}为劫财(${isYang ? '羊刃' : '月劫'})` };
  }

  // 透干集（年/月/时，排除日）
  const revealedGods = (SLOT_ORDER.filter((s) => s !== '日') as PillarSlot[])
    .filter((s) => four[s] !== null).map((s) => four[s]!.ganGod);
  const isShown = (g: TenGod): boolean => revealedGods.includes(g);

  let 格: GeName; let 立格依据: PatternResult['立格依据']; let 透干: boolean;
  if (isShown(monthGod) && GE_NAME_BY_GOD[monthGod]) {
    格 = GE_NAME_BY_GOD[monthGod]!; 立格依据 = '本气透干'; 透干 = true;
  } else {
    const zaShown = HIDDEN_STEMS[monthZhi].slice(1)
      .map((h) => tenGod(dm.gan, h.gan)).find((g) => isShown(g) && GE_NAME_BY_GOD[g]);
    if (zaShown) { 格 = GE_NAME_BY_GOD[zaShown]!; 立格依据 = '杂气透干'; 透干 = true; }
    else { 格 = GE_NAME_BY_GOD[monthGod]!; 立格依据 = '月令本气暗藏未透'; 透干 = false; }
  }

  // 备选格：月支藏干中透出且非主格者
  const 备选格 = [...new Set(
    HIDDEN_STEMS[monthZhi].map((h) => tenGod(dm.gan, h.gan))
      .filter((g) => isShown(g) && GE_NAME_BY_GOD[g] && GE_NAME_BY_GOD[g] !== 格)
      .map((g) => GE_NAME_BY_GOD[g]!),
  )];

  return { 格, 立格依据, 透干, 月令本气十神: monthGod, 备选格, 说明: `月令${monthZhi}(本气${monthBenQi}=${monthGod})·${立格依据}` };
}
```

- [ ] **Step 4: 跑绿** — `npx vitest run test/pattern.test.ts && npx tsc --noEmit` → PASS。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/facts/pattern.ts bazi-core/test/pattern.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: buildPattern 机械立格(禄刃/本气透/杂气透/暗藏未透+备选格·不judge高低)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: `cong.ts` — 从格信号（只信号不定论）

**Files:** Create `bazi-core/src/facts/cong.ts`；Test `bazi-core/test/cong.test.ts`

**精确语义：**
- 模块私有常量：`CONG_WEAK_RATIO=0.10, CONG_STRONG_RATIO=0.90, CONG_GUANSHA_MIN=0.5`。
- `r=strength.强弱比`；`dmW`。
- **从弱raw** = `r<=CONG_WEAK_RATIO && !strength.得令`。从弱raw 时：若 `strength.得地.hasRoot` → 反例 `'日主有根(得地)，假从'` + 候选 null；否则候选 `'从弱'`。
- **从强raw** = `r>=CONG_STRONG_RATIO && strength.得令`。从强raw 时：若 `五行得分[keMe(dmW)] >= CONG_GUANSHA_MIN`（官杀显著克身）→ 反例 `'盘中官杀克身，非真专旺'` + 候选 null；否则候选 `'从强'`。
- **触发项**：
  - 候选='从弱'：异党 dominant 类别（取 `食伤/财/官杀` 五行得分最高者）→ `从儿`(食伤)/`从财`(财)/`从杀`(官杀)；若并列按 `WUXING_TIE` 定序。
  - 候选='从强'：`专旺` 按 dmW：木→曲直格/火→炎上格/土→稼穑格/金→从革格/水→润下格。
- **强度** = `候选 ? round2(clamp01(|r−0.5|×2)) : 0`。
- 最终是否从格交 LLM（本模块只出信号+反例）。
- （合化生扶日主之假从 反例需 relations，cong 不取 relations，本版不做，记入设计决策。）

- [ ] **Step 1: 写失败测试**（`test/cong.test.ts`）

```ts
import { describe, expect, test } from 'vitest';
import { buildCongSignal } from '../src/facts/cong';
import type { StrengthAnalysis, EmergentTopology, WuXing } from '../src/types';

const S = (p: Partial<Record<WuXing, number>>): Record<WuXing, number> => ({ 木: 0, 火: 0, 土: 0, 金: 0, 水: 0, ...p });
const strength = (o: Partial<StrengthAnalysis>): StrengthAnalysis => ({
  月令旺衰: '死', 得令: false, 得地: { hasRoot: false, positions: [] }, 得势: { 印比数: 0 },
  五行得分: S({}), 同党: 0, 异党: 0, 强弱比: 0.5, 身强弱: '均衡', borderline: false, ...o,
});
const topo = {} as EmergentTopology;

describe('buildCongSignal', () => {
  test('从弱：r<=0.10 无根不得令 → 候选从弱，触发项按异党 dominant', () => {
    const st = strength({ 强弱比: 0.05, 得令: false, 得地: { hasRoot: false, positions: [] }, 五行得分: S({ 金: 8, 木: 0.3 }) });
    const c = buildCongSignal(st, topo, '木'); // 金=官杀
    expect(c.候选).toBe('从弱');
    expect(c.触发项).toContain('从杀');
    expect(c.强度).toBeGreaterThan(0.8);
  });
  test('从弱假从：极弱却有根 → 候选 null + 反例', () => {
    const st = strength({ 强弱比: 0.08, 得令: false, 得地: { hasRoot: true, positions: ['日'] }, 五行得分: S({ 金: 8, 木: 1 }) });
    const c = buildCongSignal(st, topo, '木');
    expect(c.候选).toBeNull();
    expect(c.反例.some((x) => x.includes('假从'))).toBe(true);
  });
  test('从强：r>=0.90 得令无显著官杀 → 候选从强 + 专旺', () => {
    const st = strength({ 强弱比: 0.95, 得令: true, 五行得分: S({ 木: 10, 金: 0.2 }) });
    const c = buildCongSignal(st, topo, '木');
    expect(c.候选).toBe('从强');
    expect(c.触发项).toContain('曲直格');
  });
  test('从强破格：得令身旺但官杀显著克身 → 候选 null + 反例', () => {
    const st = strength({ 强弱比: 0.92, 得令: true, 五行得分: S({ 木: 10, 金: 1.5 }) }); // 金=官杀>=0.5
    const c = buildCongSignal(st, topo, '木');
    expect(c.候选).toBeNull();
    expect(c.反例.some((x) => x.includes('官杀'))).toBe(true);
  });
  test('均衡 → 无候选 强度0', () => {
    const c = buildCongSignal(strength({ 强弱比: 0.5 }), topo, '木');
    expect(c.候选).toBeNull(); expect(c.强度).toBe(0);
  });
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/cong.test.ts` → FAIL。

- [ ] **Step 3: 实现 `cong.ts`**

```ts
import { keMe, sheng, ke, type WuXing } from '../constants/gan-zhi';
import { categoryOf } from '../ten-gods';
import type { StrengthAnalysis, EmergentTopology, CongSignal, TenGodCategory } from '../types';

const CONG_WEAK_RATIO = 0.10;
const CONG_STRONG_RATIO = 0.90;
const CONG_GUANSHA_MIN = 0.5;
const WUXING_TIE: WuXing[] = ['木', '火', '土', '金', '水'];
const clamp01 = (x: number): number => Math.max(0, Math.min(1, x));
const round2 = (x: number): number => Math.round(x * 100) / 100;
const ZHUANWANG: Record<WuXing, string> = { 木: '曲直格', 火: '炎上格', 土: '稼穑格', 金: '从革格', 水: '润下格' };
const CONG_ER: Partial<Record<TenGodCategory, string>> = { 食伤: '从儿', 财: '从财', 官杀: '从杀' };

export function buildCongSignal(strength: StrengthAnalysis, _topology: EmergentTopology, dmW: WuXing): CongSignal {
  const r = strength.强弱比;
  const S = strength.五行得分;
  const 触发项: string[] = [];
  const 反例: string[] = [];
  let 候选: CongSignal['候选'] = null;

  const congWeakRaw = r <= CONG_WEAK_RATIO && !strength.得令;
  const congStrongRaw = r >= CONG_STRONG_RATIO && strength.得令;

  if (congWeakRaw) {
    if (strength.得地.hasRoot) { 反例.push('日主有根(得地)，假从'); }
    else {
      候选 = '从弱';
      // 异党 dominant 类别（食伤/财/官杀 五行得分最高）
      const drainW: WuXing[] = [sheng(dmW), ke(dmW), keMe(dmW)]; // 食伤/财/官杀 五行
      let best = drainW[0]!;
      for (const w of drainW) {
        if (S[w] > S[best] || (S[w] === S[best] && WUXING_TIE.indexOf(w) < WUXING_TIE.indexOf(best))) best = w;
      }
      const label = CONG_ER[categoryOf(best, dmW)];
      if (label) 触发项.push(label);
    }
  } else if (congStrongRaw) {
    if (S[keMe(dmW)] >= CONG_GUANSHA_MIN) { 反例.push('盘中官杀克身，非真专旺'); }
    else { 候选 = '从强'; 触发项.push(ZHUANWANG[dmW]); }
  }

  const 强度 = 候选 ? round2(clamp01(Math.abs(r - 0.5) * 2)) : 0;
  return { 候选, 强度, 触发项, 反例 };
}
```

- [ ] **Step 4: 跑绿** — `npx vitest run test/cong.test.ts && npx tsc --noEmit` → PASS。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/facts/cong.ts bazi-core/test/cong.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: buildCongSignal 从强/从弱信号+反例(假从/官杀破专旺·只信号交LLM定论)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 装配进 `analyze-chart` + ChartAnalysis 4 字段 + index 导出

**Files:** Modify `bazi-core/src/analyze-chart.ts`、`bazi-core/src/types.ts`（ChartAnalysis +4）、`bazi-core/src/index.ts`；扩 `bazi-core/test/analyze-chart.test.ts`

- [ ] **Step 1: 写失败测试**（追加到 `test/analyze-chart.test.ts`）

```ts
test('计划4：ChartFacts.分析 含 十神组合/矛盾张力/格局/从格信号', () => {
  const f = computeChartFacts({ year: 1990, month: 3, day: 15, hour: 10, hourUnknown: false, gender: '乾' });
  expect(Array.isArray(f.分析.十神组合)).toBe(true);
  // 组合降序 + 成立程度合法区间
  for (const c of f.分析.十神组合) {
    expect(c.成立程度).toBeGreaterThanOrEqual(0.1);
    expect(c.成立程度).toBeLessThanOrEqual(1);
    expect(['吉向', '凶向', '中性']).toContain(c.极性);
  }
  expect(f.分析.矛盾张力.矛盾张力轴).toBeDefined();
  expect(f.分析.格局.格).toBeDefined();
  expect(['从强', '从弱', null]).toContain(f.分析.从格信号.候选);
});
test('计划4：确定性——同生辰两次 computeChartFacts 深相等', () => {
  const i = { year: 1985, month: 8, day: 20, hour: 14, hourUnknown: false, gender: '坤' } as const;
  expect(computeChartFacts(i).分析).toEqual(computeChartFacts(i).分析);
});
```

- [ ] **Step 2: 跑红** — `npx vitest run test/analyze-chart.test.ts` → FAIL（ChartAnalysis 无这些字段）。

- [ ] **Step 3: 实现**

`types.ts`：`ChartAnalysis` 追加（旧 6 字段不动）：

```ts
  // 计划 4：
  十神组合: ComboHit[];
  矛盾张力: TensionResult;
  格局: PatternResult;
  从格信号: CongSignal;
```

`analyze-chart.ts`：import 4 个 builder，在 `用神` 之后追加（顺序 pattern→cong→combos→tension）：

```ts
import { detectGodCombos } from './facts/combos';
import { buildTension } from './facts/tension';
import { buildPattern } from './facts/pattern';
import { buildCongSignal } from './facts/cong';
// ...
  const 用神 = analyzeFavor(chart.四柱, chart.日主, 旺衰, 刑冲合害, 涌现拓扑, 调候);
  const 格局 = buildPattern(chart.日主, chart.四柱);
  const 从格信号 = buildCongSignal(旺衰, 涌现拓扑, chart.日主.wuXing);
  const 十神组合 = detectGodCombos(chart.四柱, chart.日主, 旺衰, 通根);
  const 矛盾张力 = buildTension(涌现拓扑, 十神组合, chart.日主.wuXing);
  return { 通根, 旺衰, 刑冲合害, 调候, 涌现拓扑, 用神, 十神组合, 矛盾张力, 格局, 从格信号 };
```

`index.ts`：计划3 块后加：

```ts
// ===== 计划 4：L1-B 锚点结构 =====
export { detectGodCombos } from './facts/combos';
export { buildTension } from './facts/tension';
export { buildPattern } from './facts/pattern';
export { buildCongSignal } from './facts/cong';
export { categoryOf } from './ten-gods';
export type {
  GodComboKind, ComboPolarity, ComboMember, ComboHit,
  GeName, LiGeBasis, PatternResult, CongSignal,
  TensionAxis, SameSourcePoint, TensionResult,
} from './types';
```

- [ ] **Step 4: 跑绿（全量）** — `npx vitest run && npx tsc --noEmit` → 全绿（72 + 计划4 新增，tsc 净）。

- [ ] **Step 5: commit**

```bash
git add bazi-core/src/analyze-chart.ts bazi-core/src/types.ts bazi-core/src/index.ts bazi-core/test/analyze-chart.test.ts
git -c user.name="妖祀" -c user.email="ningningningning0420@gmail.com" commit -m "$(cat <<'EOF'
feat: 装配 combos/tension/pattern/cong 进 ChartAnalysis(L1-B 锚点结构完成)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review（写后自查）

- **规格覆盖**：combos(Task2-3)/tension(Task4)/pattern(Task5)/cong(Task6) 四段全覆盖；装配+类型(Task7)；共享 categoryOf(Task1)。规格「模块装配顺序」favor 之后追加 pattern→cong→combos→tension，不破依赖（favor 只吃 topology+climate+strength）。
- **抗查表**：12 组合全 `gate(结构在场)×连续乘积`，制化维引入第三/四方（cp 第三神 / #11 读 #1 degree）；身强弱用 bodyStrong01/bodyWeak01 连续门；#7 硬阈值 1.5 改 ramp 连续门。Task3 含「抗查表回归」测试。
- **确定性**：force/transparency/adjacency 全有 SLOT_ORDER tie-break；round2 只在 ComboHit.成立程度 / TensionAxis.张力强度 / CongSignal.强度 输出字段；力量快照全精度；#1 raw degree 缓存（过滤前）供 #11。每模块含「同输入深相等」测试。
- **跨计划一致**：force 用 `p.ganGod`/`h.god`（日柱天干排除、月支×3，照 strength.scoreWuXing）；transparency 透干按 (slot,stem) 查 StemRoots.hasRoot、藏干不查 StemRoots；adjacency 删 刑冲合害.贴邻 破路径；tension 复用共享 categoryOf + topology.clashes.克/受/intensity；类型字段名与 types.ts 占位注释对齐（十神组合/矛盾张力/格局/从格信号）。
- **类型一致**：ComboHit/TensionResult/PatternResult/CongSignal 在 types.ts 集中；detectGodCombos(four,dm,strength,roots) / buildTension(topology,combos,dmW) / buildPattern(dm,four) / buildCongSignal(strength,topology,dmW) 与 analyzeFavor「传片段」范式一致。
- **占位扫描**：无 TBD/TODO；每实现步给完整代码；每测试步给完整测试。

## 设计决策与审核采纳（对抗审核 4-lens 结论 + 采纳）

审核总评：12 组合抗查表红线整体达标（无一退化为二元查表），骨架可作实现依据。**采纳的 P1/P2 修正已并入上面公式**：
1. **[P1] f/fn/force 记号焊死**：`force`=raw 累加（无 clamp）、`fn=clamp01(force/2)`；gate 用 `force(X)>0`；#7 旺衰 `ramp(force(七杀),1.5,3.0)`（raw 标度）。
2. **[P1] tension「有制化」契约**：combos 写 `力量快照['制化系数']`（制化维乘子全精度）；tension 用 `<=RELIEF_GATE(0.7)` 判定（非 `<1`，因 #7 乘积型恒 <1）。
3. **[P1] transparencyFactor 反查**：透干按 (slot,stem) 精确查 StemRoots.hasRoot；藏干分支不查 StemRoots（HIDDEN_STEMS role 直判）。
4. **[P1] adjacencyFactor 删 刑冲合害.贴邻 破路径**（RelationHit.members 是 Gan/Zhi 非十神，无法匹配）；纯 SLOT_ORDER 距离 + 类别代表槽(透明度降序,SLOT_ORDER升序) + `'日'` 端 + 纯藏 force 并列 SLOT_ORDER tie-break。
5. **[P1] cp 类别分派 categoryForce**（不漏同类另一十神）。
6. **[P1] #11 读 #1 raw degree**（过滤前缓存）。
7. **[P1] cong 从强补官杀破格反例**（`五行得分[官杀五行]>=0.5` → 候选 null）。
8. **[P1] builder 传片段签名**（非 facts 黑盒），装配顺序 favor 后追加。
9. **[P2 命理增强]** #3 食神制杀 `1−cp(偏印)×(1−cp(财))`（财制枭护食）；#5 财多身弱 `1−cp(印)×(1−cp(财))`（印生身被财坏，比劫救应已在 bodyWeak01，不双算）；#6/#8 食伤一路 `cp(食伤)×(1−cp(印))`（印克食伤折减）；#10 食伤通关 `SECONDARY_RELIEF×cp(食伤)` 次优折扣。
10. **[P2]** cong 从弱 dominant 落同党时不给细分（本版 dominant 只取异党五行，天然规避）；提取共享 categoryOf 消除三套漂移。

**开放问题（已采默认，可后改）**：①用神十神只取主用神（计划3 已定）。②内含矛盾键集不纳比劫夺财。③round 只在最外层输出字段。④cong「合化生扶假从」反例需 relations，本版不做（cong 信号 only、定论交 LLM），记为计划5+ 可选增强。⑤tension 元张力未校验「印双任承载」，记为 L3 知情项。

---

## Execution Handoff

计划完成。作者既定节奏 = **主控内联 TDD**（计划1/2/3 因 API 529 已用此法）：逐 Task 跑「写测试→红→实现→绿→commit」，全绿+tsc 净后派 opus 终审子代理（抗查表/命理自洽/一致性），再 ff 合并 main。
